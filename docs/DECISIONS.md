# Decisions

One line of rationale each. Newest at the bottom. Prefix D-nnn. Domain decisions carry `TODO(verify)` where a statutory value is seeded from research rather than confirmed from a primary source; see `docs/RESEARCH.md`.

## Engineering

- D-001 Operating mode is autonomous: both checkpoints in brief section 13 are removed by the user's instruction; decisions are recorded here instead of asked.
- D-002 TypeScript stays on 5.9.x, not 6 or 7, because the brief specifies 5.x strict and typescript-eslint support for 7 is not yet settled.
- D-003 Playwright is pinned to 1.56.1 because the container ships Chromium build 1194 for that version; the config also accepts `PLAYWRIGHT_CHROMIUM_PATH` for other machines.
- D-004 Routing: one Next.js App Router page (`app/page.tsx` plus `app/[...slug]/page.tsx`) prerenders every known path (static routes plus seed IDs from `generateStaticParams`) and hands navigation to a client-side router built on `history.pushState`. Reason: under `output: 'export'`, Next's own `<Link>` fetches an RSC payload per path and hard-navigates to a 404 for any path not prerendered, which breaks runtime-created referrals and meetings; the client router keeps the same path shape and never fetches. Deep links to seed entities still work because they are prerendered.
- D-005 Seed data is generated at startup by a deterministic generator (seeded PRNG, fixed default seed) rather than shipped as committed JSON; `pnpm seed:export` writes JSON for inspection. Reason: no regeneration step to forget, and `generateStaticParams` uses the same generator at build time.
- D-006 Demo "now" is fixed at 2026-09-02 09:00 Europe/London so clocks and screenshots are reproducible; Settings offers the real clock.
- D-007 Tauri is the primary shell and its config, Rust source and capabilities are written for macOS and Windows; the Linux CI container has no WebKitGTK so the Tauri binary is not built here. Electron is the verified fallback and both shells load the same `apps/web/out`. Neither shell changes the default build target because the brief allows an Electron switch only when Tauri blocks the work, and it does not.
- D-008 Workspace packages export TypeScript source directly and are compiled by Next through `transpilePackages`; no per-package build step.
- D-009 Zod 4 is imported from `zod` (the v4 API is the default export in 4.x); `@hookform/resolvers` 5.x supports it.
- D-010 Tailwind 4 is used CSS-first with `@theme` in `apps/web/styles/tokens.css` and `@custom-variant` for `[data-theme]`; no `tailwind.config.js`.
- D-011 Persistence: store changes are written to `localStorage` under a versioned key in the browser and to `@tauri-apps/plugin-store` in the Tauri shell; "Reset demo data" clears both.
- D-012 Every screen accepts a `?state=` query (loading, empty, error, restricted, offline, stale) so Playwright and demos can reach designed states without mocking internals. It is a demo affordance and stays in production builds because the mockup is always a demo.

- D-013 TanStack Table stays on the 8.21 line rather than the 9.x rewrite: the v9 API (`useTable`, feature composition) is new and its docs were unreachable through the session proxy; 8.x is stable and maintained. Upgrade path noted in HANDOVER.
- D-014 ESLint stays on 9.39 because `eslint-plugin-jsx-a11y` 6.10 declares no support for ESLint 10; the flat configs use `reactHooks.configs.flat.recommended` and `jsxA11y.flatConfigs.recommended`.
- D-015 Print packs use dedicated `--color-print-*` tokens (pure black, white and a mid grey) so Stylelint's no-hex rule holds outside tokens.css while print stays black on white.
- D-016 Decorative hairlines (`line-1`, `line-2`) are exempt from the 3:1 component check; a dedicated `line-3` token at 3:1 is used for every input and control boundary. The contrast script enforces `line-3`.
- D-017 Screenshots are written to the repository `docs/SCREENSHOTS` from an absolute path in `tooling/playwright`, whatever the Playwright working directory.
- D-018 Next.js telemetry is disabled through `apps/web/.env` (`NEXT_TELEMETRY_DISABLED=1`) so no build or dev command phones home.

- D-019 Chronology visibility per view: single-agency shows the user's own agency events; integrated shows events with `integrated` visibility plus the user's own agency events; the pack view shows integrated events of moderate or high significance only. Restricted events show only to full-access members of the restricted process.
- D-020a Pattern lenses are pure functions in `packages/domain/src/chronology/lenses.ts`, each returning event ids, spans and a finding that always ends with the same "prompt, not a conclusion" sentence.
- D-020b The add-event schema rejects a fact whose title or detail contains opinion markers ("I think", "seems", "probably") and points the user to an analysis note; an analysis note must link at least one fact.
- D-020c Print packs paginate in JS (18 rows a page) because Chrome cannot render page counters in fixed running headers; each page carries the marking, reference, subject and page number.
- D-020d Connector pulls run through TanStack Query with the mock latency (200 to 1500 ms) visible; pulled events land in the inbox as pending and never in a chronology.
- D-020e Every Person record view writes a `read` audit entry and every chronology export writes an `export` entry; restricted reads use `read-restricted`.
- D-024 `TableWrap` renders a labelled, focusable region (`role="region"`, `aria-label`, `tabindex=0`) because wide tables scroll sideways and WCAG 2.1.1 needs a keyboard way to scroll them. Cost: one extra tab stop per table. Callers pass `label` where "Table" is not descriptive.
- D-025 Scenario seeders are ordered 01 to 08 in `SCENARIOS` and run before the bulk population, so scenario ids are stable and never collide with generated ids. Seeders may not edit `build.ts`; wiring is a separate step.
- D-026 Admin and Reports render with the context drawer column closed (`data-chrome='wide'`), because configuration and returns have no selection for the drawer to describe and the tables need the width. Chair mode uses the same mechanism with the rail collapsed too.
- D-027 `tokens.css` uses `@theme static` so every design token is emitted to `:root` whether or not a stylesheet references it. Tailwind 4 otherwise tree-shakes unused theme variables, and agency and print colours are applied through inline `var()` lookups, which silently fell back to `currentColor` (the MARAC report's health bar rendered as ink).
- D-028 Working-day clocks skip the national bank holidays and the council's own holidays; the two lists are separate configuration (`bankHolidays`, `councilHolidays`) and both are editable in Admin. The national list follows the gov.uk bank holidays feed (Scotland division) and is re-read yearly.
- D-029 A clock rule may count back from an anchor (`direction: 'before'`). For the CPPM notice rule the trigger instant is the meeting date, so the clock reads "due 5 days before the meeting" without a second date field.
- D-032 Electron is the demo build. Tauri stays fully configured as the target shell for macOS and Windows and is built on those platforms (amends the "Tauri primary" reading of D-007 for the demo only).
- D-033 Admin's default theme and density apply on a device that has no appearance preference of its own yet; once someone chooses in Settings, that choice wins.
- D-037 Scottish bank holidays are loaded from a committed copy of the gov.uk bank holidays feed (`packages/domain/src/config/bank-holidays.json`, Scotland division only) and refreshed with `pnpm holidays:sync`; the dates were verified against the feed on 03 Sep 2026, including the one-off 15 June 2026.
- D-038 `@playwright/test` is pinned to 1.62.1 (supersedes the 1.56 pin in D-003). The Linux build container cannot download its Chromium through the proxy, so the suite runs there against the preinstalled Chromium through `PLAYWRIGHT_CHROMIUM_PATH`; on macOS or Windows run `pnpm exec playwright install chromium` once. ESLint stays on 9.

## Design

- D-020 ink-3 darkened to `#6F6759`, risk-high to `#B4400F`, risk-medium to `#8A5306` so every text colour passes 4.5:1 on every paper step; see DESIGN.md section 2.
- D-021 Two agency colours added: regulator (OPG, MWC, Care Inspectorate) and fire and rescue, because scenarios 1, 5 and 7 need them and neither fits an existing agency.
- D-022 No avatars, initials circles or photos anywhere; identity is name, date of birth and reference. Safeguarding and design reasons in DESIGN.md section 9.
- D-023 Pills are reserved for process and stage; agencies use marks (glyph + colour + label).

## Domain

- D-040 The IRD label is configurable per local area; default "Inter-agency Referral Discussion (IRD)" per the 2021 national guidance.
- D-041 Clock rules live in `packages/domain/src/clocks/rules.ts`; every rule carries `source` and `confidence` from `docs/RESEARCH.md`. Rules marked Verify or Local are `TODO(verify)` in code and are shown in Admin with their confidence.
- D-042 Need-to-know defaults to deny; where the brief's matrices are silent, the more restrictive reading is used (for example a referrer receives an acknowledgement and outcome only, never detail).
- D-043 MARAC perpetrators and their associates, and MAPPA victims, are hard exclusions in the need-to-know rules: no detail level, not even presence, can be granted to them through the UI.
- D-044 MAPPA records are `restricted` classification; non-members see a RestrictedState with break-glass (reason required, 4 hour window, audited). Break-glass is not available for MARAC perpetrator-linked personas at all.
- D-045 Council officer eligibility under ASP s52 is configuration (`aspCouncilOfficerEligibility` in `packages/domain/src/config/default-config.ts`), seeded as registered social worker, or nurse or occupational therapist with the required post-qualifying training, per the Code of Practice; `TODO(verify)` against the local council rule.
- D-046 An unborn baby is modelled as a Person with `lifeStage: 'unborn'` and `expectedDeliveryDate`, linked to the mother by an `unborn-child-of` relationship, so pre-birth IRDs and CPPMs have a subject without inventing a birth date.
- D-030 The first review CPPM is 6 months from the initial CPPM per Appendix D, with no local override; a review brought forward on significant change is a meeting decision and does not change the clock. Three CP clocks were added on the product owner's values (notice 5 calendar days before the CPPM, core group escalation 3 calendar days, post-birth review 3 months advisory), each marked to verify.
- D-031 Break-glass requires a reason category from the configured list (`breakGlassReasons`) plus free text of at least 15 characters; the audit entry records both and the window (`breakGlassHours`) is Admin configuration.
- D-034 Four towns and the care home were renamed to names with no search footprint (Auchentorran, Portlennan, Dunlarrick, Craiglarrick, Whinbrae House); the checks and the rejected candidates are in `docs/RESEARCH.md` section 6. Ardvale and Clydeshore stay.
- D-035 MARAC perpetrator and MAPPA victim exclusions are keyed on case role, not person identity: each process carries a case-role register (`parties`, entries sourced from the referral, from relationship records via `partiesFromRoles`, or recorded by hand with a reason); `isExcludedParty` resolves the exclusion rows against that register and runs before membership or matrix rows in `accessFor`; `excludedUserIds` is gone; the victim and her children are never derived as associates.
- D-047 The DAQ that accompanies a MARAC referral and the MAPPA referral both ask "Is there anyone else who must not receive information about this case?" (`mustNotReceive`, shared entry schema in `packages/domain/src/forms/must-not-receive.ts`). Each answer becomes a manual case-role register entry keyed by a typed name: `casePartySchema` accepts `name` as a third form beside `personId` and `userId`, and `isExcludedParty` matches a candidate `name` against those entries case-insensitively after trimming. The parties offered are limited to the roles the exclusion rows name for that process (MARAC: perpetrator, family or associates; MAPPA: victims, employer, family or associates, public). A repeated name for the same party replaces the earlier entry rather than duplicating it. `accessFor` still checks platform users by id only; matching users by typed name is deliberately not done, because namesakes would lock out the wrong person.
- D-036 Appendix D of the 2021 national guidance, read live on 03 Sep 2026, sets the CP clocks: notice 5 calendar days before the CPPM (High), core group escalation within 3 calendar days of a significant change or concern within the plan (High), first core group within 15 working days (High), review of a pre-birth plan within 3 months of the pre-birth CPPM (High, deferrable on professional judgement after the birth), an inquorate CPPM reconvened within 10 working days (High), the CPPM record distributed within 10 working days (High). The 28 day initial CPPM clock follows a child protection investigation; the "concern being raised" framing belongs to the unborn baby row.
- D-039 Distributing a minute from the meeting workspace completes the process's record distribution clock, and the minutes pack shows the distribution deadline while it is running.
- D-058 The product does not stamp OFFICIAL on everything. Annex 2 of the MAPPA National Guidance is explicit that there is no requirement to explicitly mark routine Official information, and a product that marks every screen and every page trains people to ignore markings, which is the opposite of what the scheme is for. `marking()` returns undefined for Official and the print packs render nothing; only Official-Sensitive is marked, at the top and bottom of every printed page, in the page header on screen, on every share, and prefixed to a download's file name. Blanket marking is the common implementation mistake here and it is avoided deliberately.
- D-059 Classification is derived from the record, not chosen by whoever prints it. `classify()` in `packages/domain/src/classification` is pure and total: it takes what an artefact is about and returns a level with the Annex 2 example each decision rests on, so the Admin screen can show the rule table and the audit entry can cite it. A person may raise a classification with a recorded reason; lowering needs one of the configured roles (seeded as the Caldicott guardian and the MAPPA Coordinator) and is refused rather than applied quietly otherwise. A derived level is never lower than a linked record's, which is why the presence-only state exists. Handling instructions are local configuration, editable in Admin and marked to verify against the organisation's own information security policy, because descriptor practice varies and has changed across editions of the UK scheme. Secret and Top Secret are absent: nothing in scope reaches defence, diplomacy or national security, and the glossary says so rather than offering levels the product could never justify.
- D-057 The product is called Person360 and the hero screen is called the Person record. They cannot share a name: a user told to "open Person360" would not know whether that meant the application or one screen inside it, and a demonstration that says "this is Person360, and here is Person 360" invites the question rather than answering it. The route stays `/people/[id]` and the feature folder stays `features/person`, so nothing else moves; the screen's component and CSS module are `PersonRecord`, the captures are `person-record-*`, and the screen names itself in a kicker above the person's name. The product name lives in the catalogue as `product.name` and nothing hard-codes it, so a rebrand is one edit; the desktop shells read it from the same key as their menu text. The lantern glyph stands in for a wordmark until a mark is designed.
- D-055 A label copied word for word from a mandated return keeps that return's punctuation and capitalisation, em dashes included, and the em dash and casing checks make an exception for keys the context file flags `verbatim`. The brief forbids em dashes in the product's own voice; a statutory return's field labels are not the product's voice, and paraphrasing them would make a submitted return disagree with the template it is filling. The exception is keyed on the flag, so it never covers copy an author writes.
- D-056 A child protection register entry records concerns, not a category of registration. The 2021 national guidance says it is not necessary to identify a category when placing a child on the register, so concerns are the national construct, a child may carry more than one, and a local category is an optional free-text field for a partnership that uses one. De-registration reasons are the national statistics publication's nine coded reasons rather than free text, so the same list serves the register, the report and case closure.
- D-053 Domain vocabulary is display text, so it lives in `domain.*` keys and the enum values stay as identifiers: `agencyLabel`, `processShort`, `stageLabel`, `roleLabel`, `clockRuleLabel` and the rest in `@mas/domain` read the catalogue by id at call time, so an Admin override applies to a mark, a table and a print pack alike. The old `*_LABELS` records, `ROLE_DEFINITIONS[id].label` and the `label` and `trigger` fields on clock rules are gone from the schemas and the data. Enum values that used to be printed raw (a status shown as `in-progress` with the hyphen replaced) now have proper sentence-case labels, which is the one deliberate wording change of the migration. Access reasons are catalogue messages; register entries, audit labels and clock notes written into records at the time of an event stay as record data, like the seed.
- D-050 Every user-visible string lives in `packages/messages/src/en-GB.json` (ICU MessageFormat, nested by namespace, four levels at most, keys sorted) with `en-GB.context.json` describing where each one appears. Components render copy through `useT()` and non-React code through `t()`; both read one merged map built from three layers, the bundled catalogue, the local overrides file `en-GB.local.json`, and session overrides persisted only for the keys someone changed (localStorage in the browser, the Tauri store or the Electron app data directory in the shells). `pnpm messages:types` generates the `MessageKey` union and `MessageArgs` map from the JSON so a missing key or argument is a compile error; `pnpm messages:check` (part of `pnpm lint`) validates ICU syntax, key usage both ways, context coverage, sorting and the style rules; `pnpm messages:extract` lists literals still in source; a local ESLint rule (`mas/no-hardcoded-copy`) fails new hard-coded copy in every file that has migrated.
- D-051 `intl-messageformat` renders the catalogue directly rather than `next-intl` or `react-intl`. The app is a static export with its own client-side router and one locale: `next-intl` brings locale routing, middleware and server message loading the export cannot use, and `react-intl` adds an `IntlProvider` and component API around the same `intl-messageformat` engine without the runtime override layer the Admin editor needs. The catalogue is plain ICU JSON, so adopting either later means pointing its provider at `en-GB.json`, replacing `useT()` with its hook (the keys and messages do not change) and moving the override layer into its message loader.
- D-052 Dates and times are formatted with `Intl` for en-GB in Europe/London and passed into messages as arguments, never written into them. CLDR abbreviates September as "Sept" in en-GB, so `formatDate` in `@mas/messages` takes the day, month number and year from `Intl.DateTimeFormat.formatToParts` and supplies the three-letter month itself, keeping the brief's dd Mon yyyy exactly.
- D-049 Dates are typed, not picked: `DateField` in `packages/ui` takes text in dd Mon yyyy (also accepting 2 September 2026, 02/09/2026 and ISO), normalises it on blur, hands the form a yyyy-MM-dd calendar date and shows a format error with the example when the text is not a real date. Native `<input type="date">` renders in the browser's locale, which the product cannot control, so it is not used anywhere; the twelve date inputs (form dialogs, meeting due dates, admin holidays, the audit filter) all use the field. Every date field carries the format and an example as its hint; in a compact row (the audit filter, the meeting due and review dates, the admin holiday lists) the label carries the format instead, as "Due (dd Mon yyyy)", and the hint is suppressed so the row stays level.
- D-048 The MAPPA annual report carries Annex 3 Tables 1 to 9 of the MAPPA National Guidance for the year 1 April to 31 March, with the field set marked High on the product owner's instruction: Tables 1, 3, 4, 5, 6, 7 and 9 are computed from the record store, Table 2 from a civil order register added to the MAPPA detail (`orders`: SOPO, RSHO, FTO, SHPO, SRO or notification order, with made and expiry dates, court and status), and Table 8 (serious further offences) shows "Data not held" in every cell because the store holds no charge or conviction data. Figures that the store cannot supply (the rate per 100,000, wanted RSOs, breach convictions) also read "Data not held" rather than zero. The wording of every title and row is a placeholder in `apps/web/features/reports/mappaAnnex3.ts`, keyed by row id, so the supplied text replaces it in one file without touching the model. Corrected on 04 Sep 2026: the wording stopped being a placeholder on 03 Sep 2026, when the product owner supplied the annex text verbatim and it went into the catalogue under `reports.mappaAnnex3` with 114 of its 125 keys flagged `verbatim`; this entry, HANDOVER section 2 and the waiting list went on saying otherwise for a day and are corrected. `mappaAnnex3.ts` holds the shape and the figures are keyed on row ids, as before.
- D-060 The workbook export refuses rather than overwrites. Every Total row of the ASP data workbook and its All adults column are SUM formulas, and a literal written on top of one leaves a workbook that looks right and stops adding up the moment anyone edits it: the failure that would go unnoticed longest. `nmdsCellMap` never emits a computed cell, `formulaCells` lists them so a test can prove it both ways, and `fillWorkbook` checks the template it was actually handed before writing each cell, returning what it skipped instead of throwing. A new edition of the workbook therefore surfaces as a visible list of refusals on the preview screen rather than as a silently broken return. The template is chosen from disk rather than bundled, because the workbook is published annually, the product makes no network calls, and a copy baked into the build would go stale without anyone noticing.
- D-061 The ASP field sets are the workbook's, not ours. Every list was reconstructed from the 2024-25 technical report's glossary until the 2026-27 workbook arrived; nine of them were wrong. Harm types were eleven and are twelve, because hoarding behaviour is its own type and self-neglect is qualified "excluding hoarding behaviour". Client groups were ten and are eleven, because infirmity or frailty due to age is a group in its own right and folding it into physical disability hides the largest single group in most areas' returns. The age bands were invented and are now the workbook's twelve. Location of harm was derived by pattern-matching the adult's address on the day, which is a guess, and a guess in a national return is worse than "Not known": it is now an eleven-value field on the concern. Referral source is its own field with the workbook's thirty-three values, because five NHS rows map to one agency and three map to no agency at all. The transcription lives in `workbook-2026-27.fields.json` as data rather than code, so the copy checkers leave the workbook's own punctuation alone and the next edition is a readable diff.
- D-062 A return that names a service under investigation is Official-Sensitive. Aggregate counts that name nobody are routine Official and carry no marking (D-058), which is what an ASP data workbook return usually is. Indicators 19b and 19c are the exception: they carry the Care Inspectorate CS number and the NHS hospital location code of every service under a Large Scale Investigation, which names a specific provider being investigated. So the classification is derived from the figures rather than fixed per report kind, and a quarter carrying any of those identifiers is prefixed OFFICIAL-SENSITIVE in the file name, before anyone opens it.


## Security and cryptography

- D-063 Primitives come from the @noble packages (`@noble/ciphers`, `@noble/curves`, `@noble/hashes`, `@noble/post-quantum`) and nothing in this repository implements one. They are audited, dependency-free and behave identically in WebKitGTK, WebView2, Chromium and Node, which is the deciding property: the product ships in two desktop shells and a browser, and a build that behaved differently under Tauri than under Electron would be untestable. WebCrypto alone was rejected for that reason and because it has no post-quantum primitives; it is still what supplies the CSPRNG. `tooling/no-weak-crypto.mjs` fails the build on MD5, SHA-1, ECB, DES, Triple DES, RC4, PBKDF2, scrypt, the platform pseudo-random source and any caller-supplied nonce, anywhere in the repository including for non-security purposes, because a reviewer grepping a safeguarding product for a broken hash will not stop to read what it was used for.
- D-064 One suite, named in every ciphertext: `v1-x25519-mlkem768-aes256gcm`. AES-256-GCM for content, with `seal` generating its own nonce and offering no way to supply one. Key establishment hybrid, X25519 and ML-KEM-768 both fed into HKDF-SHA-256, so the result holds if either half holds and an implementation flaw in the newer primitive is not fatal. Ed25519 for short-horizon signatures and ML-DSA-65 alongside it for anything that must verify in decades, because an audit entry or a meeting minute from 2026 may be read in a Learning Review in 2050. Argon2id for the recovery passphrase. One HKDF info string per purpose, so a key derived for the local store cannot be used as a blind index key. The AAD binds record id, classification and generation, length-prefixed so one field cannot impersonate another, which is what stops a ciphertext being moved from a routine record onto a restricted one. Decryption dispatches on the suite identifier it finds rather than the one the code prefers, `rewrapToSuite` makes a suite change a supported operation, and a fixture sealed on 03 Sep 2026 is opened by a test on every run and never regenerated.
- D-065 "Ensure that everything is end-to-end encrypted" is not implementable here and is not quietly reinterpreted either. The design encrypts every record to exactly the set of people entitled to see it, rather than encrypting everything to one organisational key, and says so in the product and the documentation. Three reasons the literal reading fails: the controller must answer subject access requests, sheriff court orders and scrutiny functions, so a system whose operator genuinely cannot decrypt would not be deployable; the ICO's encryption guidance treats data made inaccessible by encryption as a likely failure of Article 32 and possibly a breach in itself, and in safeguarding a locked-out practitioner at three in the morning is a child protection incident rather than a service level; and encrypting everything to one key defends against a stolen backup and nothing else, when the breaches that actually happen here are the curious colleague, the unentitled agency and the administrator with standing access. `docs/THREAT-MODEL.md` states the claim exactly and `docs/SECURITY.md` section 10 is the rule that no screen may exceed it.
- D-066 Entitlement is a key, not a boolean. The resolver returns the list of principals a record is wrapped to instead of answering "may this user see this?", so the need-to-know matrix and decryptability are the same fact. `canSee` was deleted rather than deprecated; `accessRank` replaces it for ordering and display and carries a comment saying it must never gate content. The rule is enforced by a test that reads every `.ts` and `.tsx` in `apps/web` and `packages/domain`, strips comments and fails if anything calls `canRead(` or `canSee(`, because a rule this easy to break by accident needs a check rather than a convention. Principal identifiers are opaque and prefixed by kind, never names or staff numbers, since they are visible to the operator. Presence-level readers are deliberately not wrapped, because presence is metadata and wrapping them would hand them the content; escrow is always wrapped, because a record escrow cannot reach is a record the controller cannot produce on a sheriff's order.
- D-067 Audit is signed, not encrypted. It exists to be read by the people it polices, a Caldicott guardian, an APC lead officer, an inspector and, if it comes to it, the ICO, and encrypting it to them would be backwards. Each entry carries its predecessor's hash and the actor's signature, so an alteration breaks the entry's own signature and a removal breaks the following link; verification stops at the first break because everything after it is unverifiable and a cascade would bury the one that matters. Actor, action, target, classification and timestamp stay in the clear and only the free-text detail is encrypted, to a set of oversight roles, since that is the field where a practitioner might write about a third party. The Admin screen verifies a deliberately tampered copy as well as the real ledger: a verification screen that has only ever said "verified" proves nothing.
- D-068 Escrow is two of five over Shamir's Secret Sharing across GF(256), with the five holders seeded in five different organisations, and `escrowDecision` refuses two holders from the same organisation in code rather than in policy, because policy is not what runs at two in the morning. `gfMul` is a loop rather than a log table lookup, so no multiplication indexes a table with a secret. Every use records a purpose from a fixed list, a lawful basis and a written reason, is signed by both holders so neither can later say they were not there, and notifies the remaining three. Recovery from a lost passphrase goes through the same path with the recovery purpose rather than a quieter one. Two colluding holders can read anything; that is a governance control, not a cryptographic one, and it is stated without hedging in the threat model, in `docs/SECURITY.md` and on the screen itself. Raising the threshold trades against availability, which is the same trade as the offline grace period.
- D-069 Search is client-side over what the signed-in user can decrypt, and reports how many records it covered and how many it could not, because a search that quietly covers less than expected is worse than one that says so. A blind index (HMAC-SHA-256 under a client-held key) supports exact match on reference numbers and on dates of birth bucketed to the month, and nothing else: it reveals equality, so names are excluded entirely and the date is bucketed to blunt a frequency attack. Encrypted search is not solved here and no screen says it is. Rejected: a searchable symmetric encryption scheme with better leakage properties, because every one of them leaks something and naming the trade-off plainly is worth more to the buying side than a scheme nobody in the room can evaluate.
- D-070 Connector data is encrypted where it already is, not where it arrives. A gateway runs inside each agency's own network holding that agency's key: it pulls from EMIS Web, ECLIPSE, iVPD or SEEMIS, maps to the event taxonomy, encrypts and pushes ciphertext, and the source system's credentials never leave the agency. That second half is the part an NHS information governance lead cares about most, because the obstacle to these integrations is rarely the data flowing out, it is being asked to give a supplier a service account on a clinical system. In the mockup the boundary is structural rather than deployed: the mock adapters run gateway-side and a test asserts the mock API receives only ciphertext.
- D-071 The offline grace period is 72 hours, seeded and configurable, and the remaining validity is shown rather than left to expire without explanation. Wrapped keys are cached locally under the device key so a practitioner in a house with no signal can open the safety plan, which is the normal case in this work rather than the edge case. The cost is stated: a device stolen while unlocked keeps its cached cases until the period expires. Shortening it narrows that window and widens the chance of a lock-out at three in the morning, and there is no value that avoids both.
- D-072 "What the host can see" is a screen, not a paragraph in a policy. Every information governance conversation in this sector reaches "who at the supplier can see my patients' records", and the honest answer has always been a document. Both panels render from the same store, the left through `serverView` which reads only what a server must hold and the right through `openProcess` which needs a key that unwraps. It shows the real metadata in full, including the classification and the opaque identifiers of every key holder, because a version that showed only ciphertext would be a lie and the one person in the room who knows that is the one who needs convincing. `docs/CRYPTO-INVENTORY.md` is generated from the source and checked for drift in `pnpm lint` for the same reason: a hand-written inventory is wrong the first time somebody adds an algorithm, and the NCSC's migration timeline makes a discovery inventory the 2028 deliverable, which this product meets at first release rather than migrates to.

## Review round (03 Sep 2026)

- D-073 RESTRICTED is not a classification and is gone from the product. It was abolished on 2 April 2014, when the six-tier Government Protective Marking Scheme (Unclassified, Protect, Restricted, Confidential, Secret, Top Secret) was replaced by the three-level Government Security Classification scheme; Official absorbed everything up to and including it and there is no direct mapping between the two. Annex 2 of the MAPPA National Guidance, which this repository already quoted correctly, names three levels and no fourth. `ClassificationLevel` is now `official | secret | top-secret`. Secret and Top Secret are in the type and unreachable: nothing in public protection casework reaches defence, diplomacy or national security, so `classify()` never returns them and a test asserts it over every subject shape the function accepts. They are present rather than omitted because a reviewer in a police or health board information security role needs to see that the scheme is the real one and not a two-level invention, and a type that offered only Official would read as though the author had not met the scheme. The glossary carries the abolition as its own entry, because a document that still uses the word as a marking is working to a scheme that has been gone for over a decade and someone should be able to look that up.
- D-074 Classification and access restriction are two properties and are now two fields. The old enum put them on one scale, which could not say that a MAPPA record is Official-Sensitive **and** access restricted, that an ASP case conference minute can be Official-Sensitive and not restricted, or that an aggregate report is neither. `Classification` is `{ level, sensitive, handling }`, where `sensitive` is the Official-Sensitive marking (a marking on a subset of Official, not a fourth level), and `AccessRestriction` is `none | restricted` beside it. Every record that was `restricted` becomes Official-Sensitive with `accessRestriction: 'restricted'`, which is what those records always meant. The word restricted now appears in three unrelated senses in the codebase, so the glossary spells all three out and the enum comment names them: access to a record; a chronology event's visibility; and the flag on an audit entry recording a restricted read. `ChronologyEvent.visibility` and `AuditEntry.restricted` are correct in their own terms and were left alone.
- D-075 The area's handling configuration is keyed on a marking profile rather than on a classification. `official`, `official-sensitive` and `access-restricted` are the three combinations an area needs to say something different about, and naming the third for what it is stops the configuration reintroducing the conflation the split removed. `profileClassification` builds the preview for the Admin screen from the profile, and the screen says in as many words that a profile is not a classification.
- D-076 The additional authenticated data gained an encoding version rather than a regenerated fixture. Sealing binds the classification, so the split changed what that field means: v1 carried one value that conflated the classification with access restriction, v2 carries `level/sensitive/restriction`. The field layout did not change and the meaning did, which is exactly when a domain separator has to move, so `person360/v1/aad` became `person360/v2/aad` and `open` tries every known encoding newest first, as suite dispatch already accepts an older suite identifier. The committed fixture from 03 Sep 2026 is **not** regenerated: `suiteCompat.test.ts` now asserts that it fails under v2 and succeeds under v1, and that a record sealed today succeeds under v2. A compatibility fixture regenerated when it fails is a fixture that tests nothing.
- D-077 Every share carries the classification of what was shared, captured at the moment of the share rather than resolved from the source at render time. `SharingRecord`, `InformationRequest` and `LawfulBasisRecord` all hold a `Classification` and an `AccessRestriction`; `Process` had the field and nothing downstream of it did, which meant the requirement that every share carries its marking was true of the record and false of the share. Captured rather than resolved because the source can be raised afterwards and the record of the decision has to say what actually went out under what marking: that is what the recipient acted on and what an inspector will ask about. A test over the whole seed asserts that no share is weaker than its source, in classification or in access restriction, because a quiet downgrade is only visible once someone looks for it and by then the share has been read.
- D-078 A recipient whose role may not receive Official-Sensitive content sees the marking and the reason they are on the list, and not the content. Marking and reason rather than nothing at all: a recipient who can see that something exists and why they were told can ask for it through the proper route, where one who sees an empty screen telephones the social worker. The roles are configuration (`officialSensitiveWithheldFrom`), stated as an exclusion rather than a permission list because a permitted-roles list is wrong the first time a role is added and nobody extends it, and wrong in the direction that withholds information from someone who needs it. Seeded from the roles that in this product only ever receive presence-level information and marked `TODO(verify)` against the partnership's own information sharing agreement, which is what actually decides this.
- D-079 `Person.ethnicity` is deleted. It was populated, which made three documents and two returns wrong, but not in the way that looked most likely: the values were not ethnicities. The bulk generator wrote the name pool's `origin` into it, so every person given a Scottish-sounding name carried "scottish" and every person given a Polish one carried "polish". That is a protected characteristic inferred from a name, which is the thing safeguarding data governance exists to prevent, and one report was already computing a minority ethnic figure from it. So the resolution is deletion rather than reconciliation: the field is gone from the schema, the seed and the generated data model, the MARAC return reports minority ethnic victims as not recorded like the other characteristics it does not hold, the DPIA's Article 9 list no longer names racial or ethnic origin and says instead that the field is deliberately absent, and a test walks every person in the seed and fails if one carries the key. A dead field on the most sensitive entity in the product is how a protected characteristic gets populated later by somebody who does not know why it was empty. The name pool keeps its `origin`, because that is a generator concern and describes which list a name came from rather than a fact about a person.
- D-080 A raised or lowered classification is stored on the record as well as audited. The audit entry is the record of the act; `classificationOverride` is the current state, and it carries the direction, the reason, the author, the timestamp and the id of the audit entry that recorded it. Both, not one: a ledger cannot answer "what is this record's classification now" without being replayed, and a field cannot answer "who changed it and why" at all. The join means a reader looking at a raised marking can reach the entry. The audit entry is written first so the record can cite its id.
- D-081 One function decides a record's classification. `effectiveClassification(config, record)` returns the override where there is one and the derivation otherwise, along with both, and every screen, print pack and export reads it. That is the failure a derived-plus-override model invites and the reason to close it in code rather than in review: one screen showing the marking a person set and another showing the one the rules give, on the same record, with nothing to say which is right.
- D-082 Two refusals are enforced in `overrideDecision` rather than in the dialog's disabled state, because the disabled state is not what runs when somebody scripts a change. A lower needs one of the configured roles. And an override may never be weaker than the strongest classification of any record it links to: that is the inheritance rule that stops a person record being lowered while it is linked to a MAPPA case, and it is the reason the presence-only state exists at all. The linked-record refusal binds a named role too, so the Caldicott guardian cannot lower a record below its link either.
- D-083 The override lives on the process and not on a share. A share carries a classification captured at the moment it went out (D-077), and letting somebody override a snapshot would defeat the capture: the point of the field is that it says what was actually shared. Person records have no stored classification to override, because theirs is derived at render from what they link to, which is what makes the inheritance rule work.
- D-084 The exclusion register stays an exact match, and gains a warning layer beside it. `isExcludedParty` compares hand-recorded names case-insensitively after trimming, which is exact string matching on the mechanism that stops a MARAC perpetrator receiving information about the case: "Ryan Kerr" does not match "Ryan James Kerr", "R Kerr", "Kerr, Ryan" or a copy carrying a non-breaking space, and the failure is silent and towards inclusion. Making the match fuzzy was rejected: a fuzzy match that silently excludes the wrong person is its own failure, and the register is deliberately a list somebody wrote down. So a similarity check runs alongside it. Adding a recipient, an invitee, a distribution entry or a pre-meeting request whose name resembles a register entry blocks behind a confirmation that quotes the entry as written, the date it was recorded, the party role and the reason, because a warning that only says "this looks similar" gives a person nothing to decide with and they will click through it. Both answers are audited with the entry that matched, so a wrong call is traceable rather than invisible. The check runs in reverse too, when a register entry is recorded, because an exclusion often arrives after the sharing has started and nothing else in the product would notice. The threshold is 0.82, seeded to catch the four documented cases and leave unrelated names alone; a deployment should make it configuration. It is a prompt and not a guarantee, and the threat model and the handover both say so.
- D-085 Escrow refuses a holder who is themselves an excluded party on the record being opened, in code, with the same shape as the same-organisation refusal and the same audit entry. The scenario is remote, the check is cheap, and the case-role register already answers the question, so refusing costs nothing and closes it; leaving the holders to notice it about themselves closes nothing. A holder is matched by account, by person record or by name, because the register records people all three ways, and a share held by a role with nobody named against it cannot be checked at all, which the screen says rather than implying it was. Recovery carries the same check the other way round: rewrapping a user's keys would hand back everything they held, so `casesWithheldFromRecovery` names the cases they are an excluded party on and those are not restored. The exclusion outlived the device.
- D-086 The internal names stay. The repository is `mas-platform`, the workspace packages are `@mas/*`, the Rust crate is `mas-platform-desktop`, the session key in local storage is `mas.session` and the desktop menu channel is `mas-menu`. None of them is user-visible: renaming them touches every import in the repository, invalidates every saved session and every screenshot path, and buys a reviewer nothing they can see. What a council installs is `Person360`, which is what `productName`, `mainBinaryName`, `build.productName`, `artifactName` and the window title all say, with the bundle identifier `uk.person360.app` in both shells. Recorded here so the next person does not spend an afternoon on it and so the inconsistency reads as a decision rather than an oversight.
- D-087 The browser device key is a residual risk in its own right and is listed as one. `docs/SECURITY.md` section 2 already recorded that in a browser the device private key lives in `localStorage`, but it was absent from both places that exist so nobody has to read for the residual risks. It is now in the threat model's collected list, in the security document's, and in the DPIA's residual risk table, stated plainly: a browser has no equivalent of the OS keychain, so any cross-site scripting flaw in the application reads the key and with it every wrapped key cached on that device. That is a materially different risk from the keychain path rather than a smaller version of it, which makes the desktop shells the supported deployment and the browser build a development and demonstration build. The Security page in Help now says that in the product, in the words somebody would use, rather than leaving it in a table two documents away.
- D-088 The dialog centres itself explicitly, at class specificity, and a build step reads the compiled CSS to check it still does. Every modal in the product rendered pinned to the top left at every width, and nothing caught it: the dialogs opened, the backdrop painted, the widths were right and focus went where it should. The cause was not in this repository's CSS. Tailwind's preflight emits `*,::after,::before,::backdrop { margin: 0 }`, which zeroes the user agent's `dialog { margin: auto }`, and a native dialog centres by resolving `auto` margins against its two zero insets. So the fix is not a reset override but a positive statement of the geometry on the component itself, `position: fixed; inset: 0; margin: auto`, which a universal selector cannot outrank. `tooling/dialog-margin-guard.mjs` then reads `apps/web/out` after a build and fails if no rule sets all three together, or if any dialog-named selector sets a zero margin, because the failure was invisible to every test that existed and a source-level lint would not have seen it either: the source was never where the problem was.
- D-089 There is one dialog primitive and no local variants. Native `<dialog>` opened with `showModal()`, for the top layer, the backdrop, the platform focus trap and Escape, and three regions on a `minmax(0, 1fr)` grid capped at `100dvh` minus the inset, so a form taller than the viewport scrolls its body and keeps its footer on screen. `showModal()` does not lock the page behind, so the primitive does, counted rather than set, and paired with React's own effect cleanup rather than with the element's `close` event: a dialog can leave the page without ever firing one, and a page locked by an element that no longer exists cannot be scrolled again. A size scale of four, `sm` for confirmations, `md` for most forms, `lg` for statutory ones and `full`, replaces per-dialog widths. `ConfirmDialog` is the yes-or-no shape, destructive in tone by default and never in wording, because the caller supplies the verb.
- D-090 A long form's validation failures are summarised at the top of the dialog body and take focus. Per-field messages alone are not enough on a form taller than the viewport: the three-point test does not fit in 700px, so a keyboard user who submits and is refused has to scroll the body hunting for red with nothing telling them what to look for. `formErrorSummary` walks the react-hook-form error tree, which nests because the schemas do, and the summary is an `alert` that says what is wrong in one place. It replaces nothing: the per-field messages stay, because the summary says what and the field says where.
- D-091 The contact sheet embeds one image per capture instead of two. It carried each screenshot twice, a 420px thumbnail for the grid and a 1280px copy for the lightbox, and 420px of a 1440px capture is 29 percent of native: enough to recognise a screen you already know, not enough to read one you do not, so telling two similar captures apart meant opening both. Raising the thumbnail to a legible width on top of the second copy went past the 16 MB the page has to fit in. Measured rather than guessed: 720px thumbnails plus the 1280px copies came to 15.3 MB, and 800px came to 16.2 MB. Dropping the second encoding pays for the first. One 1280px image now serves the grid card, where the browser downsamples it to a 440px column and the text stays crisp, and the viewer, which reads it from the card's own element rather than from a JSON blob that would put every capture in the file twice again. The page fell from 12.6 MB to 10.9 MB in the process, so the gallery did not need splitting by phase.
- D-092 Four named layout modes, chosen by width in one module, applied through one attribute. The layout broke below 1440 for a single structural reason: the grid's column widths came from media queries in the stylesheet and the components' own states came from the appearance store, so the two could disagree and did. At 1100px the rail's track was 72px while the rail still rendered its expanded contents, which want 125px, so the labels were sliced down the middle rather than replaced by icons; the drawer had the mirror of it, holding 360px at an 800px viewport and leaving the record 368px. `wide`, `standard`, `compact` and `narrow` are written to `data-layout` on the document element by `apps/web/lib/layout.ts`, and the stylesheet carries no media query for layout at all: every track is a custom property redefined per mode. A breakpoint therefore cannot be changed in one place and missed in the other, because there is only one place. A boot script writes it before the first paint, as the theme's does, so a compact viewport does not paint the wide layout once and reflow.
- D-093 The rail's icon state below 1280 is not a preference. Above it, a person can collapse the rail by hand and that is persisted. Below it, the icon rail is the only thing that fits, so the toggle is not rendered rather than being rendered and ignored, and `RailContents` takes a `compact` flag that changes what is in the markup rather than only how wide it is: the labels leave the link's accessible name and return as a title, the badge moves to the corner of the icon, the brand becomes the glyph. Below 1024 both side columns leave the page entirely and return as panels reached from the top bar, which is what carries WCAG 2.2 1.4.10: a 1280px viewport at 400 percent zoom is a 320px one, and 72px of icon rail is a fifth of it.
- D-094 The overlay rail and the overlay drawer are the dialog primitive, not a second overlay implementation. Both needed the same four things from the platform, the top layer, focus trapped inside, Escape and the page behind held still, and all four already existed in `Dialog` with a regression suite behind them. So `Dialog` gained a `placement` of `inline-start` or `inline-end`, which replaces the centring margin rather than removing it: `margin-inline: auto 0` against two zero insets pushes the panel to the end edge and keeps the block margins at `auto`, so nothing about it reintroduces the zero margin the compiled-CSS guard exists to catch. The drawer's content is computed by one hook and rendered by both shapes, because a need-to-know answer that differed between the docked column and the panel would be a second implementation of the access rules.
- D-095 Panels respond to the record's width, not the window's. `.app-content` is a size container named `record`, and the layouts inside query it. The record is 872px at 1440 with both side columns, 952px at 1024 with one, and 1312px at 1920: the number that decides whether two columns fit is the record's and the window's does not predict it. This is also what fixed the process record at 400 percent zoom, where the two column layout was collapsing correctly and the single column was still 453px wide, because a grid item's `min-width` defaults to `auto` and the full-width row's min-content was setting the track for everything above it.
- D-096 Text may wrap or it may truncate visibly, and nothing else. Wrapping is the default and is required for anything carrying a sentence; a person record's staff safety alert is the case that decides it, because "do not visit alone" with the reason truncated is worse than the untruncated version being long. Truncation is for identifiers in tight rows and must show an ellipsis and keep the full string reachable in a `title` or an accessible name. The third thing, text sliced off with no ellipsis, is forbidden because it looks like the text ended there. `Pill` enforces its half in the primitive: capped at its container's width and ellipsised, because a pill wider than the thing it sits in is a design error at that spot rather than something the layout should absorb, and one on the process record was 453px inside a 320px column and took the page sideways with it. A pill that genuinely carries a sentence asks for `wrap` and grows downwards instead. The policy is asserted on five screens rather than described in a document nobody re-reads. Corrected on 04 Sep 2026: the five screens were not the person record header, and the check looked only at leaf elements, so a pill with an icon in it was never examined. The person record's register alert rendered "On t / Child / Prote / Regis" at 1024 and 900 in a column that had collapsed to 60px, and the suite passed. `Pill` in wrap mode no longer clips at all (overflow visible, a long word breaks inside the box, soft corners so two lines read as a notice), and `layout.spec.ts` asserts the two-column headers and every pill and process mark in them at all four widths, catching a grid child narrower than 20ch with wrapped text as well as text outside a pill's box (D-200).
- D-097 A link the reader is not entitled to follow looks exactly like one they are. That is the whole point, and it is the opposite of what most systems do. A link that is styled differently, greyed out or absent when the reader has no access tells them something about the record before they have asked for it, and it leaves no audit entry because nothing happened: the reader learns "there is something here I cannot see" for free and without a record of having learned it. So `PersonLink` renders the same anchor either way, and following it lands on the presence-only state, which says a record exists, that this reader may not read it, and how to ask, and that read is audited. The difference between a system that refuses and a system that hides is that only the first can be inspected afterwards.
- D-098 An excluded party is never a link. Where a case is in scope and the exclusion register names the person for its type and stage, the name renders marked and not clickable, with the party role after it for a screen reader. A link would put a route into their record next to the sentence saying they must not be given one, which is an invitation misfiled as a convenience. The decision lives in `PersonLink` and reads the register, rather than each screen deciding for itself: the MARAC referral panel had been rendering the perpetrator as a link beside a "must not receive anything about this process" pill since it was written, and that is precisely the failure a rule enforced in one place prevents from recurring on the next screen somebody writes.
- D-099 Practitioners have a card, at `/practitioners/<id>`. Every field on it was already in the seed and none of them had a screen: a colleague appeared as a name beside a case role and nothing else, so "who is the MHO on this and can I ring them" was answered by scrolling a members list and then not answered at all. That is the wrong gap for a multi-agency product to have, because the other agency's worker is the person you actually need. The card's case list is filtered by the reader's own access rather than the practitioner's, and says how many cases it is not showing: a card that listed every case a colleague holds would be a way round need-to-know that took one click.
- D-100 Two lists, answering two different questions, and neither is persisted. The trail answers "how did I get here" as the path actually walked, most recent last, truncating rather than appending when a record already on it is revisited, because a loop is not a queue. Recently viewed answers "what was I working on", in view order, most recent first. Both are session state: a trail restored from local storage after a week claims a journey that did not happen, and a list of the people whose records this account opened is not something to leave on a shared council laptop for whoever signs in next. The consequence is deliberate and visible in the test: reloading the page empties both.
- D-101 A clock links to the rule that sets it, and the rule opens from the address rather than from an effect. "Why is this five working days" is the question a sceptical practitioner asks first, and the answer, the source and the confidence, is in the timescale rule's own dialog. `/admin/timescales?rule=<id>` opens it. The open rule is derived from the query rather than copied into state on mount: an effect that calls setState on arrival is a second render for no reason and makes the address and the screen disagree the moment anyone presses back.
- D-102 The person network's list is visible and its diagram is decorative. The list used to be visually hidden, an accessible alternative to an `img`-role SVG, which was correct as far as it went and meant that the one screen showing who is around a person offered no way to reach any of them: a name in an SVG `text` element cannot be a link, so a sighted reader who wanted the sister's record went back to search and typed it. Now everyone gets the same list of links and the SVG is `aria-hidden`, which is also less to maintain: one source of the names rather than two.
- D-103 The process record says who the case is about, as against who is working it. The two were conflated: the participants panel lists practitioners, and the people the case actually concerns were visible only inside the drawer's need-to-know section, so a reader looking at a MARAC record could not see the perpetrator's name without opening a panel about something else. The case-party register now has its own panel, every name a link except the excluded ones, which is also what makes D-098 reachable rather than theoretical.
- D-104 The sign-in screen is one surface, and the honest statement is the second thing on it. The screen it replaces was two panes that stopped: a paper-coloured left half ending at 597px with four hundred pixels of nothing in it, and a bare right half carrying thirteen equal organisation cards. `docs/NOTES.md` has the full critique, written before the rebuild, because a rebuild without a written account of what was wrong is a redecoration. The rebuilt screen puts the masthead across the window with its contents on the body's own column so there is one left edge, and moves "this is a demonstration, every person, case, address, postcode and number in it is invented" from a footnote in the least looked at corner to a marked block immediately under the headline. It is the truest thing the screen has to say and it was set in the smallest type in the worst place.
- D-105 One field filters organisations and people together, and it takes focus on arrival. The screen's whole job is choosing who you are, so the fastest route in should be to type a name: "moira" narrows the organisation list to the Health and Social Care Partnership, selects it, and leaves Moira Gilmour as the only person on the right. Two lists behind one field rather than a search on each, because the reader does not know or care which of the two their word belongs to. Focusing an input on arrival is usually rude and is right here, because there is nothing else on the screen to read first.
- D-106 Both questions are visible at once, and the organisation is a list rather than a grid of cards. Thirteen equal cards across two thirds of the screen made the least interesting decision anybody makes here the largest one, and answering it pushed the persona list below the fold at 900px, which on a projector is a scroll in the first ten seconds. A single column list of thirteen costs 20rem and leaves the people the room they need. The chosen organisation is derived rather than held in state where the filter has removed it from the list, so there is never a frame where the two columns disagree, and no setState on arrival.
- D-107 The persona used last time is offered before either question is asked. `mas.lastPersona` was already written on sign-in and read on arrival, and the tag it drove appeared only after the organisation had been chosen, so the one person who most wants to get straight in, the person demonstrating this for the fourth time, still made both choices. It is now a card at the top with a Continue button. Still no credential field: a password box that accepts anything is a lie told in the first five seconds of a demonstration about honesty in information sharing, and there is nothing here to authenticate against.
- D-108 The demo clock is settable, and one instant drives everything. It was a fixed constant with a live toggle beside it, which meant the clocks were the part of the product hardest to show standing still: "the inquiry decision is due in three days" is a number on a screen, where moving the clock four days and watching the same clock go overdue, its band turn red, the worklist reorder and the home count change is the demonstration, and it is the thing a room of practitioners will actually test against what they know. `session.nowIso` holds it, `useNow()` is the only reader, and every statutory clock, countdown and relative date is computed from it. Moves are absolute rather than accumulated, so a jump forward and a jump back are the same operation and repeated moves cannot drift; setting it turns the live clock off, because a demo instant the real clock overwrites a second later is not a setting. An unparseable value is refused by the store rather than silently taken, and `parseDemoNow` still falls back to the seeded instant rather than returning an Invalid Date, because a screen full of "NaN days remaining" is a worse failure than a clock that did not move.
- D-109 The Playwright sign-in helper seeds a session only when there is not one already. It wrote `mas.session` unconditionally on every navigation, which quietly overwrote everything the application had put in the session since: a persona switched mid-test came back on the next `goto`, and the demo clock returned to the seeded instant the moment a test navigated after moving it. That is also what a person does: sign in once and keep the session until they change it. The bug was invisible until something other than the user id started living in the session, which is the usual shape of a test helper that is a little too helpful.
- D-110 One write pipeline, and the consequences of a write live in it rather than at each call site. `docs/FUNCTIONALITY-AUDIT.md` counted twenty-nine places that mutated the store and forty-seven that wrote an audit entry, with no rule connecting the two. That is how a product grows a fifteenth create path that forgets to start a clock, and the failure is silent: the record is there, the screen looks right, and the statutory deadline nobody is counting arrives anyway. `store.write()` runs the ten steps of `docs/RECORDS.md` section 7 in order. A caller says what it is writing and why; the pipeline decides what else must happen. Corrected on 04 Sep 2026: when this was first recorded the pipeline existed and the new create paths used it, but the raw `upsert` stayed public and eighteen pre-existing call sites (the four statutory forms, the meeting workspace, the chronology, the person's own views, actions, the worklist, the inbox, sharing and the connector pull) still wrote directly, skipping the audit entry, the classification check, the exclusion check, the rewrap, the clocks and the chronology while this entry and the handover said otherwise. Every one of them now goes through `write()`, `upsert` is private to `store.ts` (nothing removes, so there is no remover), and `apps/web/lib/write.test.ts` walks every source file in `apps/web` and fails on a direct write outside the store. The migration landed several commits after the pipeline, and `docs/NOTES.md` says why the gap existed.
- D-111 Refusals are total and come before anything is written. The schema, the business rules the schema cannot express, the reason a change after the fact has to carry, the classification downgrade check and the exclusion register are all evaluated before the record reaches the store, so a refused write leaves no half-record, no orphan audit entry and no clock counting down against something that does not exist. Everything the pipeline then does is collected into the result rather than being invisible, so a screen can tell the practitioner what happened, "one clock started, one chronology event written, three people now entitled to read this", instead of each screen having to know in advance.
- D-112 The caller says what kind of chronology event it is; the pipeline decides whether one is written. An address move is `move.address` and a household change is `household.change`, and a pipeline that guessed would file every write under one taxonomy entry. What it enforces is the part that gets forgotten: that the event exists at all, with the right author, agency, instant and process links, and only where the change is a significant one. The significant-event list is in `docs/RECORDS.md` so the distinction is a decision rather than an accident.
- D-113 Step 9, the outbound connector proposal, is a seam in this commit rather than an outbox. The pipeline takes the proposals and reports them, and the outbox with its delivery state, its origin stamp, its idempotency key and its human authorisation is step 14, which is where the honest per-connector capability matrix lives too. Recorded here rather than discovered at fifteen call sites when step 14 lands: the shape the callers pass is fixed now, so adding the outbox behind it changes one function.
- D-114 There is no direct create for a person. The path is search, review candidates, then create only if nothing matches, and the create button is not on the screen until the practitioner has said in as many words that none of the candidates is the person. Two records for one child is the failure a multi-agency system makes worse rather than better: the two records end up with different agencies who each believe they have the whole picture, and it is a named finding in review after review. A search offered alongside a create button is a search most people walk past on a Friday afternoon, so the search is the only door. The assertion is then recorded on the record itself as `createdAfterReviewing`, a count, because "created after reviewing three candidates" is a thing an inspector can ask about and a practitioner can be held to, where "the search was shown" is not. Editing any search field after the candidates have been dismissed sends the flow back to the start, so the number on the record always describes the search that was actually reviewed.
- D-115 The candidate the searcher cannot see is the one that matters, so it is shown rather than hidden. A duplicate usually gets created precisely because the existing record was invisible: the person is on a MAPPA case, the searcher is a health visitor, the search comes back empty and the obvious next move is to create a second record. The duplicate search therefore runs over every person in the product, and a candidate the reader has no access to is rendered with its own state, a dashed edge and a sentence saying the person may already be known and to ask for access rather than create a second record, with a link to where requests live. No detail beyond the name and the date of birth, which is what presence-level access already permits, and consistent with D-097: refusing visibly and auditably beats hiding.
- D-116 Duplicate matching is explainable, and every candidate says why it matched. `findDuplicateCandidates` returns reasons rather than a score: same CHI number, similar name, same names in the other order, recorded under an alias, same date of birth, date of birth with the day and month swapped or two digits transposed, born the same year, same current or previous address. A practitioner deciding "is this the same child" needs the evidence, not a percentage, and a system that shows a percentage is asking them to trust a number they cannot check. The matcher knows the mistakes that actually produce duplicates: Mc and Mac, hyphens collapsed, names transposed between the given and family fields, and the two date transpositions that a keyboard produces. An address match on its own is dropped, because a refuge, a care home or a large block would otherwise return everybody in it.
- D-117 Permission to create is checked at the action and the refusal carries a route. A user who cannot create a thing does not see a form they cannot submit, and the refusal names why and what to do instead: a regulator is told to send a referral to social work, a system administrator is told to ask the allocated practitioner. `canCreate` is shaped on the role's oversight kind rather than on a list of role names, because a list of role names is wrong the first time a role is added and nobody remembers to extend it. The system administrator is refused everything but a chronology event, which is D-070 held to: the account that edits the need-to-know matrix does not also create the records it governs.
- D-118 A generated CHI number is deliberately not a valid one. A real Community Health Index number is ddmmyy, a three digit serial, a ninth digit odd for men and even for women, and a check digit computed on a modulus 11 weighting. `syntheticChi` builds the shape and skips the check digit rule, so every number the product holds fails validation in any system that actually checks one. A number that looks right and validates right is a number somebody eventually pastes into a live system. The serial is passed in rather than drawn inside, so the seed generator and the create form are both deterministic without the function holding state.
- D-119 The write pipeline returns codes and the screens do the wording. A refusal is a fact about the record rather than a piece of copy, the store has no translator to hand, and the same refusal has to be assertable in a unit test with no React tree around it. So `store.write()` returns `reasonRequired` and `excluded:Ryan Kerr`, and `apps/web/lib/writeErrors.ts` turns them into sentences from the catalogue next to the dialogs that show them. An unrecognised code is passed through to the screen rather than swallowed, because a refusal a practitioner cannot see is a Save button that does nothing, and a Zod issue already reads as a sentence.
- D-120 A create is audited as a create, not as an edit. `create` joins the audit acts. An inspector asking when a record came into existence and who put it there should not have to infer it from the earliest edit on a record that has been edited a hundred times since, and the audit ledger's act filter is the place that question gets asked.
- D-121 The dialog margin guard matches the dialog box rather than the word "dialog". It failed on a paragraph inside a dialog body that reset its own margin, because CSS modules compile every class in `AddPersonDialog.module.css` to a name containing "dialog" and the guard matched the word anywhere in a selector. It now matches the `dialog` element or a hashed class ending `__dialog`, which is the box itself. The regression it exists to catch is unchanged, and a guard that cries wolf is a guard people learn to run with their eyes shut.
- D-122 A merge repoints by walking the dataset, and records the path of every reference it moved. There are more than twenty places a person id can appear, several of them nested three deep inside a process detail, and a hand-written list of them is wrong the first time a schema gains a field. So the merge walks, replaces, and keeps the dotted path of each replacement, which is what makes the unmerge exact rather than a reconstruction: it sets those paths back and nothing else. The walk returns the same object wherever nothing changed, so an untouched record stays referentially equal and the overlay persists only what actually moved. The audit ledger is the one collection it never rewrites, because the ledger records what happened and a merge that edits its own history is not an audit trail.
- D-123 A merge keeps both records whole rather than reversing a field union afterwards. `unionPerson` fills the survivor's gaps from the retired record, the survivor's own values winning wherever both hold one, because somebody chose which record survives and that choice is the answer to "which of these two is right". The merge record then stores `survivorBefore` and `mergedPerson` as they were. Trying to invert a union afterwards means deciding which of two identical values came from where, which is not answerable, so it is not asked.
- D-124 An undone merge keeps its record. `undoneAt` and `undoneReason` are set and the merge stays in `personMerges`. The merge happened, somebody decided it, and a product that deletes the evidence of a mistake when the mistake is corrected has no way to answer "has this record been merged before". Both the merge and the undo write a chronology entry on the surviving record, under a new `record` event family, because a chronology that showed only the merge would leave a reader believing the join still stands.
- D-125 The merge dialog shows the consequences as a count and a list, computed, not as a warning triangle and a sentence about being careful. "Fourteen references move to this record" is something a practitioner can check against what they expect. "This cannot be undone" would be untrue here, and "be careful" is not information. The comparison table runs the real `unionPerson` rather than describing what it will do, so what the reader sees is what they will get, and a value the surviving record gains is marked with a rule as well as a tint.
- D-126 The person picker is the create path's search, not a second one. A picker with its own matching would be a second answer to "is this the same person" and the two would drift, so `PersonPicker` runs `findDuplicateCandidates` and shows the same reasons. Adding a person from inside it opens the create dialog nested rather than sending the user away with a half-filled form behind them, and the nested dialog is the same dialog with the same mandatory search: there is no quick path that skips it.
- D-127 A whole-dataset change persists through a diff rather than a bespoke overlay write. `upsert` and `remove` are the right shape for almost everything, because almost everything writes one record; a merge is not. `persistDatasetChange` diffs the two datasets by object identity, which the merge's walk makes reliable, and writes exactly the records that moved. A record that comes back after having been removed, which is what an unmerge does, is taken off the removed list as well as written, because `applyOverlay` removes after it upserts and would otherwise filter it straight back out. Step 13's closures and deaths will want the same function. The unmerge is written to match: it rebuilds only the subtrees that hold a recorded path and returns the same object for every other one, because the obvious clone-and-assign implementation would put every record in the product into local storage on an undo.
- D-128 Household membership is dated, so `memberIds` is gone. Removing somebody sets an end date rather than deleting them from a list, because who lived where and when is exactly what a chronology needs and it is the question a review asks first: was the mother's new partner living there in the March before the injury. A plain list of current members cannot answer it, so there is no plain list. `members` carries `personId`, `from`, an optional `to` and the reason it ended; `membersOn(household, date)` is the only way to ask who was there, and the seed derives each member's start date from their own address history for that address so the two can never disagree.
- D-129 A relationship is stored once and read from both ends. Recording that Kayleigh is Lily's mother puts "Kayleigh Docherty, mother" on Lily's record with no second record to drift from the first, which is what the "automatic inverse" rule is really protecting against. `inverseOf` gives the type the same fact reads as from the other side and returns null where the vocabulary has none, because the reverse of "attorney for" is a description rather than a relationship type and a guess there would be worse than an honest gap. The parent types are deliberately many-to-one: the inverse of "mother of" is "child of", and the inverse of that is "parent of", because the child's side does not record which parent it was. That asymmetry is an argument for one record, not two.
- D-130 The consequences of a relationship change are computed and shown before the save button. `exclusionChanges` runs the real party register with and without the proposed record and diffs the excluded sets, so "recording this will mean Marion Fraser does not receive information about the MARAC" is the product's own rules speaking rather than a sentence somebody wrote next to a form. The MARAC case is the one that matters: the register derives a perpetrator's family and associates from relationship records, so an exclusion happens without anybody typing the word, and that must never be a surprise.
- D-131 A household change names the open cases it touches and offers to tell them, with the sharing records generated from the matrix. The question is not "what processes is this person joining on", it is "what open processes does anybody in this household have", because a new adult in a household where a child is on the register is a fact the core group needs. Ticking the box runs `resolveNeedToKnow` for each affected process and writes one share per entitled recipient with the lawful basis the matrix names, so the basis is never invented at the point of sharing. The box is ticked by default and can be cleared, because it is a decision and not a formality.
- D-132 Ending a relationship never lifts an exclusion by itself, and the decision is asked either way. The register reads the relationship record rather than its dates, so a former partner stays excluded when the relationship ends, which is the safe behaviour and the right one: a former partner is frequently the whole risk. What the product must not do is let either direction pass silently, so ending a relationship that carries an exclusion asks explicitly, defaults to it standing, and writes the answer onto the case with a name, a date and a reason. "Stands" is not a no-op: it moves the entry from derived to written down, so it survives whatever happens to the relationship next. "Does not stand" sets `stands: false` on an explicit entry, which suppresses the derived one, and it is the only thing in the product that lifts an exclusion.
- D-133 The write pipeline no longer assumes every collection carries a classification. It cast the record to a `ClassifiedRecord` and read the marking, which is right for a process and wrong for a household, a relationship or a person: those have no marking, because it lives on the process and the records it links to. On the first household edit the check read `undefined.handling`, and the failure mode was the worst kind: three dialogs stayed open with no error on them and nothing on screen said the save had failed. `isClassified` now guards it, and a test pins the three cases.
- D-134a A picker mounts its create dialog only while it is open. A closed `<dialog>` is hidden, so leaving it in the markup looks free. It is not: a person record now hosts three person pickers, so three copies of the whole create flow sat in the DOM with duplicate test ids and duplicate controls on every one of them, which is what a strict-mode locator found. Mounting on demand is also what the dialog primitive expects, since its open state is where the focus handling lives.
- D-134 A sheet head wraps rather than overflowing. `.actions` had `flex-shrink: 0` and the head had no `flex-wrap`, so two small buttons beside a title in a 280px column left the sheet entirely and were drawn over whatever sat next to it. Nothing looked wrong; the symptom was a button that could not be clicked because another sheet's head intercepted the pointer. The head now wraps, the title block may shrink and the actions may shrink with their buttons wrapping their own text, which is what a 320px column needs: at 400 percent zoom on a 1280px viewport, "Add someone to the household" does not fit on one line.
- D-135 Eligibility and permission are two gates, and both give reasons rather than hiding the option. Eligibility comes from the person, permission comes from the persona, and the ways out differ: an ineligible person needs a different process, an unauthorised user needs a referral to the agency that can. Every process is listed on the start dialog with its answer, including the ones that are not available, because a greyed option that explains itself teaches the product where a missing one leaves a practitioner concluding it cannot be done. The statutory floors are the ones the Acts set rather than the ones that feel right: adult support and protection and adults with incapacity from 16, child protection to under 18 including an unborn baby, MARAC an adult victim process.
- D-136 MAPPA has no age floor and is not given one. Annex 3 Table 6 of the national guidance carries an "Under 18" age band for registered sex offenders, so under-18s are managed under MAPPA and blocking it on age would be wrong in a way a coordinator notices in the first minute. Where the subject is under 18 the product warns and cites the Children (Care and Justice) (Scotland) Act 2024, under which under-18s are treated as children across justice processes: that changes how the case is run rather than whether it can be opened. The 16 and 17 year old gets the opposite treatment and for the same reason: both adult support and protection and child protection are offered, both say so, and the choice is recorded, because the ASP national minimum dataset keeps a distinct age category precisely so it can be known which route a young adult took.
- D-137 A clock starts at the trigger its own rule names, not at whatever create is on screen, and reading the rules changed the answer. Only the two ASP clocks are triggered by an adult concern being received. `cp.cppm.initial` is triggered by the investigation beginning at the inter-agency referral discussion, so opening a child protection *concern* starts nothing; a pre-birth concern starts `cp.prebirth.cppm`. Both MARAC clocks and both MAPPA review clocks run from a meeting being held, and the MHO report clock from notification of a guardianship application. So opening a MARAC referral starts no clock, which is correct rather than an omission, and the dialog says which clocks start and, where none do, what will start them: a practitioner who expects a countdown and sees none needs to know which of the two it is.
- D-138 The seed now guarantees a 16 or 17 year old. The bulk generator drew children uniformly from 0 to 17, and across 58 households happened to produce none of that age at all, so the eligibility case the guidance singles out could not be demonstrated. Every fourth family household now has one deliberately, with the mother's age range raised so the floor is reachable rather than silently dropped. A gap in the demonstration data is worth fixing in the data: the product handled the case correctly and had nobody to show it on.
- D-139 A new process is built by a switch rather than a cast. `buildOpeningProcess` discriminates on the type once and pairs it with the matching detail, which is four more lines than a spread with a computed `type` and a computed `detail`, and means the compiler proves the pairing rather than being told. A cast there would be a claim nobody checked, on the one record shape where the product's whole taxonomy lives.
- D-140 An opening records the honest empty value rather than a plausible one. An ASP three-point test opens as `unclear` on all three limbs with the reasoning left for the person who does it, not as `no`, which would be an assessment nobody made. The workbook's own fields open as "other" and "not known": a national return computed from a guessed referral source, or from a location of harm read off the adult's address on the day, is worse than one that says the value was not recorded.
- D-141 A plan is a list of outcomes rather than a paragraph. Actions hang off outcomes and a review checks them one at a time, so a plan recorded as one block of prose cannot be reviewed in the way a review meeting actually reviews it. The dialog therefore starts with one outcome row and adds more, and each outcome gets its own id from the moment it is written so an action can point at it. The adult protection review date is asked in the form rather than left to the schema: the refusal is correct and already exists, but arriving after the practitioner has typed everything else is a worse way to learn a rule than a field that says "required" while there is still nothing to lose. Where it has been agreed that no further action is required, the tick box is the exception the national minimum dataset glossary allows, and it is the only thing that removes the requirement.
- D-142 An alert asks who can see it, and the answer is not a default anybody should inherit. A staff safety alert that only some agencies can see is not doing its job; a MAPPA presence alert seen by everybody has disclosed that a MAPPA case exists, which is the one fact the case is built to control. So the scope is two explicit choices with a sentence each saying what the choice means, rather than a list of agency tick boxes with no framing, and a scope of named agencies with nobody named is refused rather than quietly saved as visible to all. The alert also carries an end date, because a bail condition expires and a missing person is found, and an alert with no end is one still telling a visiting worker something that stopped being true three years ago.
- D-143 A granted protection order starts the order's statutory clocks, and the dialog shows the dates before the practitioner commits. The durations are not repeated in the dialog: they are already in the clock rules with their citations, so the mapping names rule ids and the rule table decides how long each order lasts. The preview runs `computeClock` against the same rules the write will use, from the date granted rather than from today, so what is shown is what will be recorded. A removal order carries two, the seven day validity and the seventy two hours to execute it, and seventy two hours is exactly the kind of fact somebody needs on the day rather than in the audit trail afterwards. A warrant for entry maps to no clock, because the rule table holds no duration for one, and a plausible guess in a statutory clock is worse than an absence.
- D-144 A MAPPA disclosure is a list of facts and it opens pending. The facts are a list rather than a paragraph because a disclosure is limited to what was decided and a paragraph cannot be checked against that limit afterwards; each line is one fact the recipient may be told. The record opens pending because the approve and decline buttons on the register are the decision, made by somebody with the authority to make it, and a dialog that recorded the proposal and the decision in one action would be recording that nobody decided anything. The recipient's capacity now has a label rather than the raw enum, which was being printed on screen beside the name.
- D-145 The case-role register gets a manual create path, because derivation cannot know everything. The register is mostly derived, from the referral and from relationships, and that is the right default. What it cannot know is that the housing officer is the perpetrator's brother, because no record links them to the case: that arrives in a phone call, and with nowhere to put it, it stays in somebody's head until the wrong person is invited to a meeting. The entry is keyed on the typed name, carries its reason, and goes through `withMustNotReceive` so it behaves exactly as an answer to the same question on the DAQ or the MAPPA referral does, replacing rather than duplicating an entry already held for that name and party. The question is read from the catalogue, so an Admin override changes it in all three places at once.
- D-146 The global create action opens the screens' own dialogs rather than a second code path. It asks what, then asks the one thing the create needs, a person or a case, then mounts the same component the record would have mounted with the same props. There is no parallel implementation to drift, so a rule added at the record is added here. Three creates are not dialogs at all: a meeting, an action and a share are made on screens that hold context the menu does not have, and the menu says so and takes you there rather than claiming a completeness it does not have. The case list offers only cases the persona can actually read, because a case reference that opens onto a restricted state is not a create path, it is a dead end. It exists because a practitioner who has just come off the phone is not on any particular screen, and a product whose answer to "where do I put this" is "find the right screen first" loses the record.
- D-147 The create menu clears itself when it opens, not when it closes. Closing is the wrong moment: the dialog primitive fires `onClose` for every way a dialog can shut, including the programmatic close that hands a create over to its own dialog, so clearing the chosen person there took the person away from the dialog that had just been given it. Clearing on the way in is where nothing downstream is still holding the state, and it also gives the right behaviour: the menu starts fresh every time rather than remembering a choice made twenty minutes ago.
- D-148 Every casework record carries a version history and a recorded-in-error marker, spread into its schema from one place. `correctable` in `schemas/common.ts` adds `versions` and `recordedInError` to fourteen collections at once, so the two questions "what has changed on this record" and "should this record exist" are answerable of a household and a plan and not only of a chronology event, which happened to get them first. Both are optional because the seed predates them and a record nobody has touched has no history to show.
- D-149 The version entry is computed by the write pipeline, not declared by the caller. `versionFor` diffs the record against the one already held, over the record's own top-level fields, so a screen cannot leave a field out of the history by not mentioning it. A caller may supply the phrase, because "Case closed" reads better than "status, closedAt, closureReason, stage and clocks changed", but it cannot supply the field list. Only values a person can read back go into the `before` map: a nested object printed into a history entry is not "what it was", it is a wall of JSON, so those fields are named as changed and their old value is left to the audit trail. A create writes no entry, because the record is its own first version and the audit entry already says who made it.
- D-150 Closure reasons come from the list the process's own national return uses, and where there is none the product says so. Child protection closes on the de-registration reasons from Children's Social Work Statistics; an ASP case on the actions-taken rows from the data workbook, which are already in the product as a field set pinned to the workbook; MAPPA exits by the three kinds already in its schema. MARAC and AWI have no national closure list this project has found, so they get a locally agreed one, marked local in the dialog and `TODO(verify)` in the code. The alternative was a fabricated national list, which is worse than an honest local one: a coded value in a national return is counted, and a value counted from an invented list is a false figure with a real consequence.
- D-151 A closure writes the coded reason where the return reads it, not only as prose. `closureReason` is a string on every process type and cannot be counted, so the closure also writes `register.deregistrationReason` on a child protection case, `inquiry.action` on an ASP case and `exit.kind` on a MAPPA case. A case that closes before registration is the exception and is handled rather than forced: there is no register to de-register from, and writing a de-registration reason onto an absent register would put a child into the national de-registration count who was never in the registration count.
- D-152 A reopened case resumes its clocks against the date they started rather than restarting them. The case was shut for a fortnight and the statutory period was running the whole time, so the deadline did not move. `stoppedByClosure` on the trigger is what makes this possible: a clock stopped because the case shut is a different fact from a clock stopped because the case conference happened, and only the first resumes. Without the flag a reopen would either restart everything, including deadlines that were genuinely met, or restart nothing, which is a deadline quietly disappearing because somebody closed a case early. The dialog says which, with the dates, because "resume" and "restart" produce very different answers.
- D-153 Recorded in error is a marker, not a deletion, and the dialog spends its space on what that means. The record stays, keeps its audit entries and stays in any pack already distributed, because a pack that went out last Tuesday went out and no later decision unsends it. What changes is that working views stop showing it and every view that still does says who decided and why. The chronology therefore filters retired entries out of the list and counts them underneath it: a list carrying entries somebody has retired misleads a reader who scrolls it, and a list that quietly dropped them misleads them differently.
- D-154 A reason is required on the fields another agency matches on and not on the rest. A telephone number changes because it changed, and asking why is bureaucracy. A date of birth, a name or a CHI number changes because it was wrong, and one changed without a reason is a record nobody can account for: the match that worked yesterday fails today with nothing to say what happened. The dialog raises the question the moment an identity field moves, and the write goes through as intent `correct` rather than `update`, so the pipeline's own reason rule and the dialog's cannot come apart.
- D-155 A death is a flow with named consequences, computed by the same function that performs them. Each open case the person is a subject of closes with the reason its own return uses: the workbook's "no opportunity for further ASP intervention" for an ASP case, "Child died" for child protection, de-registration for MAPPA. A case the person is only a party to is flagged for review and not closed, because a father dying does not close his child's child protection case; it changes it, sometimes profoundly, and that needs a person to read it rather than a system to shut it. Each closure is its own write with its own audit entry, chronology entry and notifications, because a death that produced one audit entry covering four closures would be a death nobody could trace through the cases it closed.
- D-156 The one honest shortcut is a toast that opens the correction path. A newly created record can be sent to recorded-in-error from the toast that announced it, which is what "undo where it is honest" means here: not a reversal that deletes the record, but the same dialog, still asking for the reason, reached in one click while the memory is fresh. The dialog is mounted once at the root and pointed at a target through a small store, because by the time the toast is on screen the dialog that created the record has closed and unmounted.
- D-157 The write capability matrix is honest, per connector, and asymmetric on purpose. ECLIPSE and its peers are full two-way because the controller is the same council. CareFirst is batch, because it is legacy and no longer developed, and modelling it as a live API would be a claim about a product that does not have one. SEEMIS is a flag and a named-person alert and never narrative. EMIS Web is a coded flag plus a task, only after accreditation covering security, privacy and clinical safety with a named Clinical Safety Officer and a hazard log, and it is marked unverified rather than claimed: the NHS England IM1 pairing route is an England programme and the Scottish route runs through the EMIS Partner Programme and national contracting, which this project has not confirmed. iVPD is notify-only, because a third party product writing into a police vulnerable persons database is not a realistic ask and pretending otherwise damages everything else in the pitch. ViSOR is never. The honesty is itself the selling point: anybody who has run an integration programme trusts the supplier who says iVPD is notify-only more than the one who claims everything writes.
- D-158 The ceiling is a fact about the far side, so it lives in the domain and not in a screen. `intentsFor` maps each ceiling to the intents it permits, `acceptsIntent` is the only question a caller asks, and the write pipeline refuses a proposal the connector would not take before anything is queued. A screen deciding for itself would be a second answer to the same question, and the second answer is the one that is wrong when a connector is added.
- D-159 Every outbound write goes through an outbox with a delivery state a person can see. A silent write failure is a safety incident: if the product believes the inquiry is open in the council's system and the write failed, a worker looking only at that system sees nothing, and the assumption that the other agency knows is exactly the assumption that appears in significant case reviews. So `sent` and `acknowledged` are kept apart and only the second, carrying the far side's own identifier, justifies the sentence "written to the council social work system, acknowledged 14:32, reference CF-2026-8871". A failure is surfaced, never retried into silence, and the attempt count survives a retry so a connector that fails every time is seen to fail every time. Parking a failure is a decision somebody made rather than a queue that grew.
- D-160 Nothing writes automatically. The system proposes, a person authorises, and the authorisation records a purpose and a lawful basis exactly as a share does, because that is what it is: a disclosure into another organisation's record. The dialog shows the payload in the target system's own field names beside the value it was mapped from, because a practitioner authorising a write into a GP record should see what lands there and a mapping a reader cannot check is a mapping they have to trust. One click to approve keeps the ergonomics; the human stays in it.
- D-161 The idempotency key is built from what the write is about, not from a clock or a counter. Connector, intent and record id, so the same logical write proposed twice produces the same key. That is what makes a retry a retry rather than a second episode, what lets the far side hand the key back, and what turns the echo problem into a lookup: an inbound change carrying a key we issued is our own write returning and is reconciled against the record that produced it, never ingested as new. There is a fallback on the external reference for a source system that does not return the key, because both are cheap and missing either is expensive.
- D-162 Conflicts are resolved by a written-down authority table, never by recency. Last-write-wins in safeguarding means the most recent click beats the more informed one. So each connector has an ownership table: demographics, clinical facts and the source system's own identifiers belong to the source; the multi-agency process, its stage and who else is involved belong here, because no single source system knows about the others. A divergence on a field one side owns is answered by the table. A divergence on a field either owns, which both sides have changed, is a conflict, and it goes to a person with both values, both timestamps and both authors, and the decision is audited.
- D-163 The outbound payload is composed client-side and relayed as ciphertext, so bidirectionality does not become the hole in the encryption story. The inbound design has the agency gateway encrypting before anything reaches the platform. If the outbound path composed its payload platform-side, the platform would hold plaintext for exactly the records it claims never to see, and the feature would undo the claim. So the payload is built in the entitled user's browser from records only they can decrypt, encrypted to the target gateway's public key, and relayed. The idempotency key is deliberately outside the ciphertext, because the platform has to match an acknowledgement and a duplicate suppressed only at the far side is a duplicate that has already been written; the key names no person and describes no event. `gateway.test.ts` asserts the relayed envelope carries none of the payload, mirroring the inbound test.
- D-164 An inbound change is a process proposal, not a chronology event. They arrive on the same feed and are different things: an event is a fact to promote, a proposal is a case to open. Accepting one opens the matching process linked by the source system's own reference, so the two records are joined by something the far side recognises rather than by a name and a date. Declining takes a reason, which is audited and, where the connector supports it, written back. An echo is never offered for acceptance, because accepting it is the duplicate.
- D-165 The source system simulator is a real second system, not a screenshot. You cannot film two-way integration without showing the other side, and a still image of a partner system proves nothing about whether the wiring works. So creating an episode in the simulator writes an inbound change into the platform's queue, which somebody there accepts or declines; editing one changes what that system holds, so the divergence appears on the platform's reconciliation screen, computed by the same function that would run against a live feed. Its state is deliberately not in the `Dataset`: it is a second system that happens to run in the same window, and putting its episodes in the platform's own record would blur exactly the line the simulator exists to draw.
- D-166 The simulator is a demo affordance and the exclusion is honest rather than implied. With a static export every route is prerendered, so there is no build in which the route simply does not exist. `NEXT_PUBLIC_DEMO_TOOLS` is read at build time, the route refuses when it is off, and the link from the Connectors screen is not rendered. That is a gate, not tree shaking, and it is recorded here as such rather than described as removal. A production build sets the flag off.
- D-167 The simulator looks like a different product on purpose, and it has a neutral name. Plainer, denser, older: a menu bar it does not need, square corners, hairline borders, a status bar, a table with no breathing room and surnames first the way these systems do. The contrast is half the demo, and a viewer must never be confused about which window they are looking at. It is called "Council social work system", because putting a vendor's name on a screen in a sales demonstration is a claim about that vendor's product. Its palette is declared once at the top of its own module as `--sim-*` properties and the stylelint hex rule is relaxed for that one file, which is the same discipline the token sheet follows rather than a blanket disable.
- D-168 Looking old is a design decision; being unusable is not. The simulator meets the same accessibility floor as the rest of the product: visible focus, a keyboard path through the episode list and the record form, labelled fields, real contrast, and no axe violations. A demo affordance that a keyboard user cannot drive is one a presenter cannot drive either, on the day the trackpad misbehaves.
- D-169 Linked processes are shown on the case, because a link nobody can see is not a link. The MARAC and the child protection case carried each other's ids from the first seed and the only thing that read them was the classification floor, so the strongest argument the product makes, that the woman at the MARAC and the two children on the register are one situation, was true in the data and invisible on screen. The Connected cases panel sits above the parties, names the other case by its reference and its stage, and is a link. It runs the access rules per linked case rather than assuming a reader entitled to this one is entitled to that one, which on a MARAC and a child protection case is frequently false: the panel says a case exists and refuses the rest.
- D-170 Presence means a case exists, not who it is about, and one predicate now decides that everywhere. Four places tested `level !== 'none'` and treated presence as enough: the process list named the subject in its subject column, the process header used the case title, which for a MARAC reads "MARAC referral, research and action planning for Kayleigh Docherty (repeat referral)", the header's next-meeting link carried the same name again, and the context drawer showed the lawful basis purpose, the exclusion register and the reads. On a MARAC the victim's name is precisely the thing that must not reach somebody who is not on the case, so this was the need-to-know model failing at the one case built to demonstrate it. `identifiesSubject` in `packages/domain/src/permissions/access.ts` is now the only answer to "may this reader see whose case this is", with a domain test pinning it, so a fifth screen cannot get it wrong a fifth way.
- D-171 The drawer refuses in the same words as the record it describes. It keeps the two sections that are about the reader, the level they hold with its reason and the marking that governs onward handling, and drops every section written about the case, with a sentence saying so. The headings go with them: a column of empty headings would still tell a reader how many things exist and how much they are missing. This matters more here than anywhere else in the product, because the drawer is the screen a practitioner is taught to trust for the need-to-know answer.
- D-172 Search indexes what the reader could decrypt, and it is built by decrypting rather than by checking a level. `readableCaseIds` is the set of cases whose content actually opened, and every case-bound record hangs off that set, so a chronology entry, a meeting, an action or a plan on a case the reader holds no key for never enters the index. Filtering afterwards is what you write when the plaintext was available all along, and it would demonstrate the opposite of the claim. A case is still matched on its reference, because a reference is not a name and refusing visibly beats hiding, and the row that comes back names the process type and nothing else. It is matched on its title only where the reader can open it, because a MARAC title reads "MARAC referral, research and action planning for Kayleigh Docherty (repeat referral)" and indexing that for everybody would put the victim's name in the one place the model exists to keep it out of.
- D-173 What the index could not reach is counted, never described. "Three cases were not searched. You hold no key for the content, so none of it reached this index." is the only honest sentence available: a real client-side index knows how many wrap lists it is not on and knows nothing at all about what is inside them. Counting matches inside those cases would mean reading them, which is the thing being refused. The line sits under the heading rather than at the foot, because a reader deciding whether to trust an empty result needs it before they conclude there is nothing there.
- D-174 Results are grouped by type on the screen and flat in the typeahead, and the two are the same order. A practitioner searching a surname is as likely to want the core group meeting or the action they owe as the person, and a single ranked list makes them read every row to find out what each one is; the group headings answer that before they start, and the count in each is what gets scanned. The typeahead keeps a flat listbox with a type tag on each row instead, because nesting `role="group"` inside a listbox for eight rows buys a heading a keyboard user cannot land on and complicates the one interaction that has to be perfect. "See all results" became an option rather than a presentation-only line: it was the one route out of a suggestion list that has not found the record, and it was reachable by mouse and by Enter and by nothing else.
- D-175 The results lag the query by 220ms and the screen says so. It is a debounce rather than a delay dressed up as one, and the loading state is the debounce showing through: instant results read as canned on video, anything past about 400ms reads as a slow product, and running the index on every keystroke of an eight letter surname is seven runs thrown away. The state goes through `ScreenState` like every other screen's, so `?state=loading` reaches the designed version of it (D-012) and a test can assert the real one on a query change, which is a pushState rather than a reload.
- D-176 Control and K focuses the search box from anywhere, and the shortcut is announced rather than only implemented. `aria-keyshortcuts` on the field and a visually hidden sentence beside it, because a shortcut nobody is told about is a shortcut for the person who wrote it.
- D-177 The demo control panel is hidden behind Control, Shift and D, absent from a production build, and says in its own subtitle that it is not the product. The gate is the same one the simulator uses (D-166): a static export prerenders everything, so `NEXT_PUBLIC_DEMO_TOOLS` refusing to render it is the removal, not tree shaking. Saying so on the panel matters because it will appear in a recording if the presenter opens it by accident, and a viewer who took it for a product feature would have been told something untrue by the demonstration itself.
- D-178 A waypoint sets the persona, the route, the theme, the density and the clock in one action, and the clock is part of it rather than an afterthought. A chapter about a review meeting on the 14th is a different chapter on the 2nd and on the 20th, so a waypoint that set everything except the instant would put the presenter on the right screen with the wrong numbers on it. The waypoints are one per row of `docs/DEMO.md`, kept as data in `features/demo/waypoints.ts` so the script and the panel cannot drift apart.
- D-179 Reset to seed takes the demo clock and any break-glass grant with it. State drifting between takes is what ruins a shoot, and those two are the state you least want carried into the next one: a grant is an audited emergency access with a four hour window, and one left standing from the previous run would make the next take look like access the reader simply had. Sign-in and appearance stay, because those are the presenter's settings rather than the demonstration's state, and the copy in Settings now says all of it.
- D-180 A snapshot is the overlay and the session, not the dataset. The dataset is a pure function of the seed and the overlay, so storing the difference is smaller, and a snapshot taken on one build still applies on the next. It exists because a take that has to be shot again needs its set-up back rather than the whole thing rebuilt, which is a different need from reset: reset throws the set-up away too.
- D-181 The two-persona panels are the real screens, drawn inside a `ViewAs` provider, not a second component showing what the rules would say. A summary would be an assertion, and worse than the assertion it replaces: it could drift from the screens it claims to describe. The override lives in a React context rather than in the store, because the store holds one session and that is correct: a panel is not a session, it is the same session looking at what somebody else would see. `useCurrentUser` reads the context first, which is why every screen works inside a panel without knowing a panel exists.
- D-182 A panel holds no break-glass grant unless it is set to the signed-in user. A grant is recorded against a process and a moment rather than against a person, because the session that took it is the only one that can hold it; passing the session's grants into a panel set to somebody else would open the record for a persona who never took one, and the view would be demonstrating the opposite of its claim. `useGrants` is now the only way a screen reads them, so a screen cannot get this wrong by reading the session directly.
- D-183 The right-hand panel defaults to a mental health officer rather than to the housing officer the brief names, because in this seed housing is a MARAC agency and Mark Hepburn is on the case. The brief's example is right in general and wrong here, and the default matters more than the example: two identical panels is the worst possible first impression of the one screen whose entire point is the difference between them. Graeme Dunlop holds presence on the MARAC, which is the contrast the screen exists for.
- D-184 The stepper's scrolling moved onto a labelled focusable region around the list, the shape `TableWrap` already used (D-024). It scrolled and could not be reached from the keyboard, which axe only found once two records were drawn side by side and the panels were narrow enough to make it overflow. The product had the same defect at 400 percent zoom and nothing had gone narrow enough to catch it.
- D-185 The recording preset is a setting, not a build. The same binary is the product and the thing being filmed, because a separate recording build is one nobody checks and the first time anybody would find out it had drifted is on the day. It raises every type size one step rather than zooming the page, so the layout stays the layout and only the reading gets easier; it forces comfortable density, because the preset is one setting rather than a reminder to change three; and it stops the motion that answers nothing. A transition that follows a click is information, so it stays at zero duration; a shimmer that loops while a viewer is being talked to is noise, and on video the codec spends its bitrate on it, so it goes.
- D-186 Every screen is swept at 1920x1080 with the preset on, with an axe pass and a check that the document never scrolls sideways. Raising the type scale changes every layout in the product at once, and the two ways that goes wrong are a heading that now wraps into a control and a region that has become scrollable and cannot be reached from a keyboard. The sweep is the point rather than any assertion in it.
- D-187 Placeholder copy fails the build. Everything a person can read lives in one catalogue, which is what makes the check possible at all rather than a grep across every component hoping the string was not built from three variables. The words are matched as words, so a person called Todd is not a TODD, and the one message that says "placeholder" on purpose carries `placeholderWord: true` in its context entry: the exception is written down beside the copy rather than as a pattern in the checker that would quietly cover the next one too. "TBD" on a screen in front of chief officers is not a small thing and nobody notices it in the room.
- D-188 `docs/DEMO.md` is a shooting script rather than a walkthrough: ten chapters, each with its waypoint, persona, exact clicks, narration, what the viewer should be looking at and a duration, and a runtime table that adds up. Chapters rather than a running order, because a ten minute video is watched once and a ninety second clip gets forwarded to whoever decides, and every chapter has to stand up on its own.
- D-189 The script is checked twice, by a script and by a test. `pnpm demo:check` reads the narration out of the Say column and fails a chapter whose words no longer fit its slot at 150 words a minute with fifteen percent left for pauses and clicks, and it fails when the chapters stop adding up to the declared total. `apps/web/e2e/demo-script.spec.ts` opens each chapter from its own waypoint and asserts the thing the "Look at" column tells a viewer to look at. Together they mean the script, the panel and the product cannot drift apart without something going red. "Timed" has to mean something or it is a word in a heading.
- D-190 The dry run corrected the script and not the product, which is what the brief asks for in that order. Chapter 5 was written as "click through to the children's case", and the MARAC coordinator holds presence on it: she can see it exists and cannot open it. That is a stronger beat than the one first written, because it is the need-to-know model working along the chain rather than the chain merely being clickable, and the chapter now shows the refusal and then the same reference opening for the social worker who is on the case.
- D-191 Section 5 of the handover is generated by `pnpm contact-sheet` rather than maintained. It is the same list as the contact sheet without the pictures, and by the time anybody looked it was missing eleven capture rounds: every round since the layout work had added screenshots and nobody had added the rows. A list of files that has to be kept in step with the files by hand is a list that goes out of date, so the generator that already reads the folder writes it and the section says so at the top.
- D-192 The bank holiday feed is a maintenance-time input and never a runtime dependency. The application must never fetch `https://www.gov.uk/bank-holidays.json`: brief section 2 requires it to run fully offline on a locked-down council or NHS laptop, and a working-day clock that silently degraded because a health board's proxy blocked gov.uk would be a statutory deadline computed wrongly, found out at an inspection. So a maintainer runs `pnpm holidays:sync`, reviews the diff and commits it, and the product reads a committed file in every environment, forever. This is written down here because a future reader finding a hard-coded holiday list will assume nobody thought about keeping it current.
- D-193 `pnpm holidays:sync` merges and never replaces, and applies nothing silently. The feed is a rolling window that gains future years and drops old ones, so an overwrite would quietly change every historical working-day computation, including ones a practitioner has already worked to. New dates are merged and listed; a date that vanished from a year the feed still carries is kept and reported, because a holiday disappearing is either a feed correction or a feed error and the script cannot tell which; and a changed date or title on an already-committed year is refused unless `--apply-changes`, which also writes a dated note into the fixture's `corrections` log. The script refuses to write a list shorter than the one committed.
- D-194 A working day is not the absence of a bank holiday, and three lists compose to decide it: the national list, which of those the organisation observes, and the council's own local days. Councils and health boards do not universally close on every bank holiday and neighbouring authorities differ on their local ones, so a product that treated the gov.uk list as a council's calendar would produce deadlines practitioners know are wrong. `isWorkingDay` in `packages/domain/src/calendar` is the only implementation and every clock calls it. `ClockRule.calendar` names whose calendar a rule counts against, defaulting to the council's; `everyDay` exists because `marac.research.return` counts backwards across agencies that include Police Scotland, which operates every day of the year. Per-agency calendars are modelled and not populated, so the shape is right when somebody needs it.
- D-195 The calendar knows its own bounds and refuses outside them rather than guessing. A weekends-only fallback looks exactly like a correct answer, which is why it is the dangerous behaviour rather than the safe one, so `dayVerdict` throws `CalendarCoverageError` outside the committed range. The clock computation is the one caller that catches it: it falls back to weekdays only and marks the result `unverified`, and the countdown then carries a note saying the calendar does not cover the period. A practitioner should never see a confident countdown computed from data that does not exist. Tests pin both boundaries.
- D-196 Holiday dates are opaque `YYYY-MM-DD` strings, compared as strings, and never round-tripped through a local-time `Date` constructor. `new Date('2026-01-01')` is UTC midnight and `new Date(2026, 0, 1)` is local midnight, and the difference moves a holiday across a day boundary exactly once a year in a way that passes every test written in summer. The one place a date becomes a `Date` is inside the weekday lookup, and it is constructed with `Date.UTC`.
- D-197 The holiday lists moved out of Admin, Defaults into an Admin, Calendar section of their own. Three lists that compose, a twelve month combined view and a working-day calculator do not belong in a page of miscellaneous settings, and the calculator is the reason the screen earns its place: a sceptical practitioner will test the product against a deadline they already know, and a calculator that shows which days it skipped and why is either convincing or wrong in a way somebody can point at. It calls the same function the clocks call, so it cannot agree with something the product does not do.
- D-198 The pipeline expresses everything the migrated call sites had been doing by hand, rather than the call sites keeping any of it. It writes the lawful basis from a purpose and a necessity (`lawfulBasisFor`: one rule for the article 6, 9 and 10 conditions and the statutory gateway, where three screens had carried drifting copies), writes sharing records at each recipient's detail level with the classification captured at that moment, completes clocks as well as starting them (`clockTransition`: a meeting names its type and the transition table decides, a distributed minute names the rule it completes), derives whether the audit entry is restricted from the case, takes a pre-allocated audit id so a classification override can cite its own entry, lets the one deliberate lower through under its own act, and notices a hand-recorded register entry being added and runs the exclusion check in reverse against every name already on a list for the case, ledgering the update and any resemblance. Small meeting changes are writes too: an attendance mark or an agenda item moved on now carries a ledger line and a version entry, where the old direct upsert carried nothing.
- D-199 A connector delivering what a source system said is the other writer, and it is not a person's write. `store.receive` is the delivery path for connector events and inbound changes: the schema check and de-duplication on the far side's own reference, no audit entry, no version, no clock and no milestone, because those belong to the review that promotes, accepts or dismisses the item, which is a `write`. It is the only other caller of the private `upsert`, and the simulator, the inbox pull and the connector sync all go through it.
- D-200 A two-column record header keeps at least 28ch for the identity column, and below compact it stacks to one. The person record's header was `minmax(0, 1fr) auto` with the record actions row pinned to column 2, so the auto column took the width of five buttons and the identity column took what was left, which at 900 was 60px. The floor is `minmax(min(28ch, 100%), 1fr)`, the narrow mode is one column with the actions row back in it, and the process record header, which has the same shape, gets the same rule. The meeting workspace header is a wrapping flex row and the practitioner card already stacks under a container query, so neither needed it.
- D-201 The nephew in scenario 1 is Duncan Fraser. He carried the presenter's name, which appeared in the concern summary, the three-point test reasoning, the inquiry rationale and the s7 and s8 visit notes, with the three-point test dialog in chapter 9 of the script. Duncan is in neither the persona list nor the forename pool, and the presenter's name is out of the male forename pool as well, so no generated person can carry it either. Removing a name from the pool shifts the deterministic generator's later picks, which is accepted: the scenario cast is named explicitly and is unchanged, and the captures were re-swept.
- D-202 The raw feed response is the provenance of the normalised holiday fixture, and the sync script can run from it without a network. `pnpm holidays:sync --from <path>` reads a captured response, `--captured-at` records when it was taken, and either way the response is written verbatim to `bank-holidays.raw.json` before anything is derived, so the raw file always precedes the fixture. Coverage is the span of years the feed publishes rather than the span of holidays, because the feed publishes whole calendar years: `coversTo` is 31 December of the last year, and the calendar answers for the working days after Boxing Day rather than refusing them. A committed entry that differs from the feed only in typography takes the feed's spelling quietly, and a note the fixture carries that the feed does not is kept as a maintainer's annotation; a changed title or date is still refused without `--apply-changes`. The first run against the hand-typed fixture found exactly those two kinds of drift and no correction, and the corrections log is empty.
- D-203 A guard that has checked nothing fails. The dialog margin guard reads the compiled export and used to exit clean when there was none, which meant `pnpm lint` on a fresh clone could not fail on the bug it exists for. It fails now, names the export it could not find and says to run `pnpm build` first; the commands in CLAUDE.md, the README and the handover say the same. The alternative, having lint trigger a build, was rejected because it hides a two minute build inside a command people run on every save.
- D-204 `pnpm lint` reports zero warnings, so a real one is visible when it arrives. The React Compiler was skipping five components: four forms that read `form.watch()`, which returns a function the compiler cannot memoise, and the virtualised chronology list, whose `useVirtualizer()` does the same. The forms read `useWatch({ control, name })` instead, which the compiler can reason about and which is the library's own recommendation; the list opts out with `'use no memo'`, the compiler's documented directive for an incompatible library, and because the lint rule reports the call whatever the directive says, that one line carries an explained disable beside it rather than living with a warning that trains people to ignore the next one.
- D-205 A research entry that a later reading corrected keeps its text and carries a "Superseded by" line naming the entry to read instead; nothing is rewritten to look as if it was right first time. The 02 Sep extracts are the record of what was known before the primary sources arrived, and the dated line under each says where the correction is. The verification table lives in the handover only, indexed from the research log by rule id, because two copies drifted within a day; the table is generated from `rules.ts` by `pnpm docs:verification-table` (writing the test showed the hand-kept table was eight rules short, and generating it showed the four NMDS deadline clocks had no catalogue text, so Admin was printing their keys; they have it now, and the test refuses a rule without it), and `packages/domain/src/clocks/verification.test.ts` fails when the committed rows differ from the code and when the research log marks TODO(verify) or Verify against a clock rule the table marks High without a superseding entry. The method note names the two kinds of source, the search extracts of 02 Sep and the six primary sources read or supplied since, each with its date and route, and the reconstruction of Annex 3 that had four of nine tables wrong is struck rather than corrected, so nobody reads it as a second source.
- D-206 There is no database, and nobody should propose one to fix a selector. The product is one in-memory dataset built from the seed with an overlay of changes persisted to localStorage, or to the Tauri store in the desktop shell, and that is the correct shape for a mockup whose contract is a static export that runs offline. Every collection persists in the overlay, the notifications added this round included; reset clears all of it and rebuilds from the seed; and no consequence of a write lives in component state, because the next screen cannot see component state. When a screen does not show a consequence, the fault is a selector or a missing pipeline step, never the absence of a database.
- D-207 A notification is an output of the write pipeline and of the clock engine, never something a screen composes. Step 8 of the pipeline is "who is told": the need-to-know rows for the stage (a share notification is written beside each sharing record), the write's own effects read as the difference between the record before and after (an assignment, a reassignment to both people, a completion to whoever asked, an invitation, a move, a cancellation, a distributed minute at each recipient's level, a member joining or leaving, a stage change to every member and to every matrix audience at its level, a raised marking and a break-glass to the lead, a break-glass to the escrow holders), the clock engine (a warning at the rule's `warnDays`, a breach at due, an action due, overdue and escalated), and the request flows (a research or pre-meeting or information request lands with the person or agency asked and its return goes back to whoever asked). A notification stores a kind, a pointer to its source, the recipient's detail level, the lawful basis where the thing announced reveals content, and a key; it never stores a name, a title or a fact. The sentence a person reads is rendered from the catalogue and the source record at read time, so a presence-level recipient reads that a case they are linked to changed and nothing more, and the wording is editable in Copy and labels like everything else. The key is what makes re-evaluation safe: two drafts with the same key are the same notification and the store refuses the second. The actor is never told about their own act. An excluded party on the case is never a recipient, by the same `isExcludedParty` check that refuses them as a share recipient, applied on the way in for a named recipient and per holder on the way out for a role-addressed one, and `derive.test.ts` and `notifications.test.ts` prove both. The seed carries one notification per share that was sent to a named person, unread where the share is unread, which is exactly what the old bell counted; nothing else is seeded, because everything else is a consequence of something happening.
- D-208 The clock engine is the third writer beside `write` and `receive`, and it writes two things only: notifications, and the escalation marker on an action whose owner has been silent past `config.actionEscalationDays`. It is evaluated whenever the instant moves (the demo clock, the live clock switch, a restored snapshot), after every process or action write, and once a minute while the live clock runs, and every draft it produces is keyed on the trigger so a hundred evaluations raise one warning. The standing warnings and breaches of the seeded state are in the seed itself, already read, with the escalation marker on the actions that have sat past the interval, so a fresh seed writes nothing at boot, the overlay stays empty after a reset (D-179 holds: an absent overlay is a byte for byte identical dataset), and the bell reflects what the demonstration did and not what the seed was born with; anything that falls due after that stays unread across a reload. The first draft wrote the baseline into the overlay on first sight, and the demo panel's reset test caught it: an overlay that is never empty is an overlay that cannot be asserted empty.
- D-209 Reading, marking everything read and dismissing a notification are the recipient's own state: persisted in the overlay, never audited. Reading a pointer to a record is not reading the record, and the record's own read is ledgered when the person opens it, as it always was. Dismissing hides and never deletes. A role-addressed notification is one record, and the first holder to read it reads it for the role, which is how a shared inbox behaves and simpler than a read receipt per holder for a case nobody has yet asked for; a share the recipient marks read on the Sharing screen reads its notification with it.
- D-210 The standard for "works" is a driven test: a thing works when a test creates it from nothing, drives it through the interface to its terminal state, and a second persona sees every consequence along the way. Seeded data is for the demonstration to look populated and is never the thing under test. `cross-persona.spec.ts` is the first of these and is committed before the affordance it drives exists, so it fails, on purpose, until the action create path lands; a test that passes before the feature is written is a test of nothing.
- D-211 The stage engine is the only route to a stage. `packages/domain/src/processes/transitions.ts` holds, per process type, the transitions a stage carries: what each needs the record to already hold, who may record it, what it validates, what it writes, which clocks it completes and starts, where the case goes and what the store must create beside it (a meeting, a plan and its actions, information requests, a closure, a linked case). A stage is reached by recording the decision that moves the case there and in no other way: the stepper is a view of the tables and offers the current stage's transitions as buttons, there is no stage picker anywhere, and the only writes that set `stage` outside the engine are opening, closing and reopening, which already were their own recorded decisions. Forward only by default; the way back is reopen, or an explicit return with a reason. Permission is per transition from the persona's role on the same oversight model as `canCreate` (the team leader takes the screening decision, the council officer records the inquiry outcome, the chair records the case conference outcome), and a refusal names the roles that do record it. The engine refuses what the record does not support and names the missing thing and the action that creates it: a screening decision to proceed without a three-point test says so and offers the test. Clocks are the rule table's business: a transition names the rules it completes and starts, a notice period counting back names the meeting date as its trigger, and stopping a clock because its stage was passed is a completion with a recorded note, never a deletion. Two readings of the tables were decided here rather than left to a form: the ASP inquiry decision clock completes at the screening decision, because the decision whether to make inquiries is that decision and opening the inquiry is the act that follows it; and a level 1 MAPPA case sits at notification with managed semantics, so disclosure, exit and a referral up are its transitions and it never has a meeting. The tables are the source of truth for `docs/HOW-TO.md` and for the handover's stage engine section, and `transitions.test.ts` drives every transition through a permitted actor, a refused actor, a record that lacks what it needs and a record that has it.
- D-212 An action may be given to any person the case permits, or to every holder of a role, and membership is not the test. The test is the one the share recipient check already applies: an excluded party on the case is never an owner, and a person who could not open the case (no access, or presence only) cannot be asked to work on it. Membership was considered and rejected as the rule, because the first action on a new case is how most people come to be involved in it, and a rule that refused the allocated worker until somebody had added them to the case would have every practitioner adding members to give out work. A role-assigned action sits on every holder's worklist until one of them takes it, and taking it is a reassignment from the role to the person, told to the other holders by the same notification as any reassignment. The rule lives in `packages/domain/src/actions/assign.ts` and every dialog that gives an action away runs it as the write's business rule, so the refusal reads the same on the Actions screen, the case, a plan and the create menu. Cancelling keeps the action with its reason; nothing deletes it. The escalation interval is configuration, in Admin, Defaults, beside the break-glass window.
- D-213 A meeting is scheduled through the stage engine where the tables schedule its type from the case's stage, as a plain meeting where the engine awaits the type there or has no view of it, and is refused, naming the stages, where the tables schedule it from a stage the case has not reached. `scheduleRoute` in `packages/domain/src/processes/transitions.ts` answers, once, for the three places a meeting is scheduled: the case, the Meetings screen (which asks for the case first) and the create menu. Holding a meeting is recording the decision it made: the close button on the workspace opens the outcome form of the transition the meeting's type fires from the case's stage, the store runs that transition, and the meeting is marked held with the transition it fired as a consequence. A type with no transition to fire (an inter-agency discussion, an LSI planning meeting, an AWI multidisciplinary meeting) is held with its minute opened and nothing decided on the case, and the dialog says so. The old meeting clock table (`clocks/transitions.ts`, keyed on meeting type) is deleted: it and the engine's tables were two answers to what a held CPPM does to the clocks, and the engine's is the one that also knows whether the meeting was quorate. A transition whose outcome form is not yet built cannot close its meeting, and the dialog names the transition rather than holding the meeting with nothing recorded; the CP planning meeting, the IRD and the MAPPA meeting forms land with their process types.
- D-214 The invite list is proposed by one module, `apps/web/lib/invites.ts`, for the schedule dialog and the workspace's generate button, and the people it does not seat are recorded on the meeting with the reason. A matrix row that gives an audience less than full detail, a person excluded by their case role, a person held back for a near-match confirmation, and anybody unticked by hand all reach `meeting.leftOff`, which the header shows. The workspace used to generate the list and toast a count of the excluded; the count is now names and reasons, because "2 people were left off" is the sentence that starts a complaint and "the perpetrator, as an excluded party" is the one that ends it.
- D-215 A MARAC referral opens without a risk assessment. `referral.riskAssessmentId` was required by the schema and the person-record path passed an empty string, so a coordinator could not open a referral from a new victim's record at all: the dialog offered MARAC as available and the pipeline refused it with a Zod message. A professional judgement referral is one the protocol accepts without the DASH threshold being met, and the assessment, where there is one, is attached by the assessment dialog on the MARAC screen afterwards. The field is optional; the panel and the SafeLives return already handled its absence.
- D-216 The pipeline applies a write's clock transition before it appends the write's fresh triggers. A rescheduled CPPM moves its notice period: the running `cp.cppm.notice` is completed and a new one starts from the new meeting date, in one write, and the old order (fresh triggers first, then the transition) completed the new trigger along with the old. A cancelled CPPM completes the notice period with the cancellation as its note, so no clock counts down to a meeting that is not happening. Any write that touches clocks re-reads them for notifications, not only a process or action write.
- D-217 The stepper's offer is a panel of buttons under it, one per transition the stage carries, and the drawer and the demo panel say the same thing as sentences. Each button opens the transition's own form, the schedule dialog where the transition schedules a meeting, or the dialog that already records it; a transition a meeting fires is a sentence naming the meeting it waits for, with a link to it once it is scheduled. A button this person may not press is disabled and says who may; one the record is not ready for says what is missing and offers the thing that records it, so a refusal is always a route. The forms are registered against transition ids in `apps/web/features/process/transitions`, one editor per input type, and a plan is one editor for every transition that records one (adult protection, adult support, the child's plan, the MARAC action plan, the MAPPA risk management plan), which is what keeps the rule about who may own an action in one place. A transition with no form yet is offered as not built rather than as a button that does nothing. The adult protection forms land here; each process type's forms land with its own driven spec.
- D-218 A person is on a case by the case's member list, not only by the seeded list on their user record. Home's clocks read `user.caseMemberships`, which the seed writes and nothing else does, so the review clock a practitioner started on a case they opened that morning never reached their Home. The selector reads the case's members as well; the seeded list stays as the demonstration's shorthand and is not the rule.
- D-219 Recording a decision on a case puts the recorder on it. The driven adult protection walk found the team leader who screened a concern reading the case at presence level a second later: the matrix seats a team leader at the concern and inquiry stages and says nothing about screening, and she was not a member. A person whose decision moved the case is involved in it by any reading, so `recordTransition` adds the actor to the members with their role and the transition as the reason, beside anybody the tables name (a chair, a coordinator, an MHO). The matrix stays as it is: it answers who is told and who may read, and membership is what a decision earns.
- D-220 The child protection decisions live where they are made. The IRD's seven decisions, each with its rationale, and the planning meeting's decision to register with the concerns, the core group, the lead professional and the child's plan are outcome forms opened by closing the meeting they were made in (D-213); the joint investigative interview, the medical, de-registration and the birth are recorded from the case. The planning meeting form seats the core group from the people at the meeting and nowhere else, because a core group of people who were not in the room is a list, not a group. De-registration closes the case with the national statistics publication's reason and stops every child protection clock in one decision. A pre-birth case runs the same forms: the schedule dialog offers only the pre-birth planning meeting on it, and the birth, recorded from the case by the midwife or the social worker, converts the subject and hands the pre-birth clocks to the child protection ones from the date of birth.
